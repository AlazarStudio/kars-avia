**Kars Avia - История обновлений**

## v0.1 (02.12.2024)
- Реализован функционал управления профилем, включая возможность редактирования профиля и кнопку выхода.
- Обновлен раздел "О компании" для отелей и авиакомпаний.

### v0.2 (04.12.2024)
- Доработана фильтрация по авиакомпаниям и аэропортам.
- Улучшен механизм отмены заявок.
- Внесены исправления в работу модуля шахматки.
- Добавлена возможность обновления номерного фонда с загрузкой описания и изображений.
- В раздел "Об авиакомпании" добавлен подраздел с отображением номеров.

### v0.3 (07.12.2024)
- Реализовано сохранение параметра `isAirline` в локальном хранилище для корректного отображения отчетов.
- Оптимизирована работа с выпадающими списками в `DropdownList`.
- Расширено использование `DropdownList` в интерфейсе.

## v1.0 (12.12.2024)
- Внедрены обновления на сайт.
- Переработан интерфейс бокового меню: добавлена компактная версия и соответствующий мини-логотип.
- Улучшен поиск, обеспечена корректная работа с пагинацией, а также доработана фильтрация заявок.
- Оптимизированы всплывающие подсказки.

### v1.1 (18.12.2024)
- В случае совпадения дат заявок отображается уведомление с соответствующей ссылкой.
- Оптимизирован поиск по номерному фонду с учетом количества мест.

### v1.2 (21.12.2024)
- Доработан механизм редактирования отдела при изменении сотрудника авиакомпании.
- Добавлена возможность удаления отелей для диспетчера.

## v2.0 (09.01.2025)
- Доработаны фильтры статусов заявок.
- Исправлены ошибки, возникающие при архивации заявок.
- Улучшен механизм загрузки изображений: установлен лимит в 2 МБ.
- Реализована пагинация в модулях авиакомпаний и отелей.
- Обеспечена корректная работа скролла при навигации по страницам.
- Исправлены ошибки в отображении боковой панели в шахматке.

### v2.1 (15.01.2025)
- Оптимизирован механизм подписки на события.
- Доработан алгоритм отображения звездности отелей в соответствии с государственной оценкой.
- Исправлены ошибки в отображении тарифов номеров.

### v2.2 (22.01.2025)
- Добавлена поддержка двух чатов в заявках (для гостиниц и авиакомпаний).
- Реализована возможность логирования данных для авиакомпаний, отелей и резерва.
- Расширен функционал по добавлению номеров различной вместимости (от 5 до 8 мест).
- Начата работа над системой уведомлений, реализована первичная визуальная часть.

## v3.0 (29.01.2025)
- Внедрены обновления на сайт.
- Обеспечен доступ для авиакомпаний.
- Оптимизирована работа с архивными заявками, исключено их отображение в шахматке.
- Исправлена ошибка смены статусов через модальное окно в шахматке.
- Исправлены ошибки в разделении чатов между авиакомпаниями и гостиницами.
- Обеспечена адаптация интерфейса для небольших ноутбуков.

### v3.1 (31.01.2025)
- Реализовано разделение чатов у отелей в резерве, добавлены кнопки для каждого отеля и отображение названия отеля в чате.
- Продолжена работа над визуальной частью системы уведомлений.
- Доработан механизм редактирования дат заезда и выезда до момента заселения первого пассажира из резерва.
- Внесены исправления в `SVG`-разметку (`stroke-linecap`, `stroke-linejoin`, `clip-path`).
- Скорректирована логика отображения заявок в резерве для отелей.
- Исправлена ошибка с очисткой поля продления в `ExistRequest`.

## v4.0 (04.02.2025)
- Исправлена ошибка сохранения данных при переходе между вкладками в авиакомпаниях и экипаже.
- Устранена ошибка внесения цен для номеров вместимостью от 5 мест и выше.
- Исправлены проблемы с авторизацией авиакомпаний.
- Улучшена адаптация интерфейса для авиакомпаний и корректировка стилей цен гостиниц.
- Обновление подготовлено для тестирования авиакомпаниями.
- Восстановлена логика резерва с учетом `matchesCity`.
- Реализована поддержка цен для авиакомпаний.

### v4.1 (05.02.2025)
- Доработана адаптация интерфейса: реализовано отслеживание ширины экрана и состояния бокового меню.
- Реализовано скрытие реквизитов гостиниц от авиакомпаний.
- Во вкладке "О гостинице" контактная информация гостиниц заменена на контактные данные Kars Avia.
- Реализована возможность просмотра изображений номеров.
- Оптимизированы чаты в резерве и шахматке.

### v4.2 (14.02.2025)
- Внесены корректировки в соответствии с обновленной структурой базы данных.
- Улучшена адаптация интерфейса и проверка подписок.
- Реализованы подписки для шахматки авиакомпаний.
- Исправлены ошибки в шахматке отелей, устранены пересечения заявок при изменении дат через модальное окно.
- Доработана логика пересечения пассажиров и эскадрильи.
- Исправлен баг с отображением списка пассажиров: реализован корректный скроллинг и отображение элементов.

### v4.3 (20.02.2025)
- Заменен `DropdownList` на `Autocomplete` из Material UI на страницах с эстафетой и при создании заявки.
- Исправлена ошибка, препятствующая изменению авиакомпании.
- Добавлен индикатор загрузки (`Loader`) из Material UI во все модули с асинхронной загрузкой данных.
- Реализованы уведомления после создания и редактирования данных.
- В разделе "О гостинице" доработан интерфейс слайдера номеров: затемнены стрелки на первом и последнем слайде.

## v5.0 (23.02.2025)
- В Эскадрилью при создании заявки добавлено поле определяющее является заявка квотной или резервной (сбой).
- Изменен цвет сообщений собеседника в чате.
- Для авиакомпаний добавлены запросы на изменение даты, которые отправляются в чат.
- Добавлено выделение для запросов на изменение даты в чатах заявок в эскадрильи и пассажиров.
- Редактирование даты у пассажиров для диспетчеров доступно всегда.


### v5.1 (25.02.2025)
- В пассажирах при создании заявки есть возможность добавить манифест.
- Проверены подписки при создании и изменении сотрудников.
- Если манифест не был добавлен при создании, то можно добавить позже и также можно его обновить.
- Чат с гостиницами в пассажирах появляется только если добавить их. Визуально чата нет, пока не добавлена гостиница или гостиница не выбрала какое количество гостей она может разместить.
- Исправлен баг с чатами отелей, при добавлении новых отлей, все отели были в одном чате.
- `border-radius` везде увеличен с 5px до 10px.
- При редактировании пользователей изначально input`ы заблокированы, возможность редактировать появляется после нажатия изменить, а также добавлена возможность скрыть пароль звездочками (*).
- В разделах "О гостинице" и "О авиакомпании" визуальная составляющая для НЕ админов этих организаций и диспетчера изменена, input`ы заменены на обычные строки. 
- В отчетах для авиакомпаний есть возможность выбирать сотрудника, чтобы получить отчет по конкретному сотруднику.
- В эскадрильи проверено своевременное отображение истории (была проблема с подпиской).
- Редактирование профиля через настройки.
- Добавлена маска при введении номера сотрудника и пассажира, а также в разделе об авиакомпании в контактах.

## v6.0 (09.03.2025)
- Виртуальный рендеринг списка комнат и заявок в шахматке гостиницы: устранены зависания при большом количестве комнат.
- Добавлен функционал непрочитанных сообщений: при открытии чата скролл перемещается до первых непрочитанных сообщений.
- Реализовано редактирование гостиницы с визуальным добавлением региона к городу при изменении города.
- Исправлены проблемы с поиском в списке гостиниц.
- Добавлены оповещения с фильтрацией по эскадрильи и пассажирам; в оповещениях можно переходить к соответствующим заявкам.
- Оповещения о наличии непрочитанных сообщений отображаются в виде красной точки слева у каждой заявки в списке.
- В заявках в пассажирах реализована возможность скачивания расселения, которое обновляется при добавлении или удалении пассажиров, а также возможность добавить и обновить манифест после создания заявки и скачать его.
- Скачивание и обновление манифеста вынесены в отдельное модальное окно.
- Названия ролей пользователя отображаются на русском языке.

## v7.0 (24.03.2025)
- Реализовано предварительное бронирование при создании заявки в эскадрилье: теперь возможно оформить заявку без предварительного выбора сотрудника с опцией его добавления позже.
- Добавлена возможность быстрого добавления нового сотрудника при создании заявки в разделе «Эскадрилья».
- В разделе «Пассажиры» восстановлена кнопка для создания новой гостиницы. Добавление пассажира происходит через модальное окно с последующим мгновенным размещением, а также реализована функция быстрого добавления новой комнаты для соответствующей гостиницы через отдельное модальное окно.
- Введено разделение гостиниц на апартаменты и гостиницы: для апартаментов реализовано деление комнат на студии и апартаменты, а информация о стоимости указывается в отдельном поле, вместо стандартной вкладки.
- В тарифах авиакомпаний добавлены две новые категории – «Апартаменты» и «Студия».
- Добавлено создание эскадрильных заявок через шахматку гостиницы.
- Убрана расшифровка должностей сотрудников авиакомпаний.
- Реализовано удаление отчетов.

### v7.1 (28.03.2025)
- Реализовано восстановление пароля. На почту отправляется ссылка, по которой можно сбросить пароль.
- Фильтрация сотрудников авиакомпании по должности.

## v8.0 (22.04.2025)
- Изменена структура цен в авиакомпаниях и гостиницах: в авиакомпаниях внедрен новый элемент "договор", который включает в себя цены для всех категорий номеров и указания аэропортов, к которым применяются эти цены. В гостиницах цены переименованы в "тарифы", которые теперь содержат название тарифа, категорию (одноместный, двухместный, апартаменты, студия), цену, описание и изображения. Эти тарифы теперь отображаются в разделе "Номера" вместо старых номеров из номерного фонда.
- В отчетах для авиакомпаний добавлены новые фильтры — аэропорт и должность.
- В форме создания заявки ФИО сотрудника отображается черным цветом, а должность и пол — серым.
- Изменен внешний вид списка гостиниц в соответствии с новым дизайном.
- Внешний вид списка заявок изменен: убраны все границы (border) и изменено выравнивание текста (text-align).
- Обновлен внешний вид страницы гостиницы в соответствии с новым дизайном, а также: добавлена галерея и текстовый редактор с шаблоном.
- В авиакомпаний в отделе добавлен функционал прикрепления должностей.
- Реализовано восстановление пароля.

### v8.1 (24.04.2025)
- Создание в номерном фонде множества комнат за раз.
- Перемещение заявки внутри одного номера между комнатами.
- Выделение черным цветом код аэропорта и серым цветом название, также с городом и регионом.
- В номерном фонде добавлен № слева от названия и название тарифа справа от названия номера. В шахаматке гостинице слева от названия номера добавлен №.
- Проверка ввода почты.

### v8.2 (01.05.2025)
- Цены за питание в авиакомпании перенесены в договор.
- Добавлена категория "Люкс".

### v8.3 (05.05.2025)
- Внешний вид заявки в шахматке изменен в соответствии с дизайном.

### v8.4 (25.05.2025)
- Добавлено отображение списка гостиниц в алфавитном порядке по городу.
- Реализована фильтрация списка заявок на сервере.
- Внедрена фильтрация по диапазону дат.
- Интегрирована библиотека react-date-range для компоненты `DateRangeModalSelector`, используемой для фильтрации по датам.

### v8.5 (09.06.2025)
- Функционал редактирования, создания и удаления тарифов добавлен в аккаунт гостиницы.

### v8.6 (16.06.2025)
- Для описания тарифа добавлен редактор текста.
- В тариф добавлено поле "квадратура".

### v8.7 (17.06.2025)
- Во вкладке "О гостинице" номера были переработаны в соответствии с новым дизайном.
- В компонент `Autocomplete` для аэропортов добавлен город, улучшая поиск и отображение данных.

### v8.8 (21.06.2025)
- Создание папки hooks. 
- Логика вычисления ширины экрана для адаптива перенесена в `useWindowSize`.
- Работа с localStorage перенесена в `useLocalStorage`.


## v9.0 (24.05.2025)
- При создании и редактировании тарифов добавлена возможность выбирать главное фото для обложки.
- Улучшен текстовый редактор.
- Обеспечено корректное отображение форматированного текста из редактора.
- В настройках гостиницы внедрён переключатель, позволяющий гостинице самостоятельно размещать заявки.
- При выборе гостиницы для размещения заявки учитывается её переключатель: если опция включена, заявка автоматически отправляется и закрепляется за выбранной гостиницей.


### v9.1 (14.07.2025)
- Patch Note в crm, просмотр, добавление и редактирование.


### v9.2 (14.08.2025)
- Создана страница "ГК Карс". Отображение, создание и изменение компаний.
- Изменение структуры договоров в авиакомпании. При создании договор содержит название и компанию. Один договор включает в себя n-ое количество соглашений, соглашения содержат Цены и аэропорты.
- Создана страница "Договоры".
- Создана страница "Аналитика". Количество созданных заявок, количество отмененных заявок и заявки по статусам по выбранной авиакомпании за выбранный период.
- Добавлена пагинация во вкладке пользователи (диспетчеры).
- Оптимизирована шахматка гостиницы. Оптимизировано получение данных.
- Оптимизирована шахматка авиакомпании (сотрудники авиакомпании). Оптимизировано получение данных.
- Статусы в шахматке гостиницы отображаются в виде выпадающего списка.


### v10.0 (19.09.2025)
- При переходе из «Эскадрильи» к шахматке выполняется автоскролл до нужной заявки; заявка дополнительно подсвечивается.
- Создана страница «Обновления». Обновления отображаются в виде статей с древовидной структурой; поддерживаются создание, редактирование и просмотр.
- Создана страница «Инструкции». Документация по CRM представлена как статьи с древовидной структурой; поддерживаются создание, редактирование и просмотр.
- В разделе авиакомпаний возвращён прежний вид отображения договоров.
- Обновлена страница «Договоры»: реализовано разделение на договоры авиакомпаний и договоры гостиниц.
- Шаблон создания договора для авиакомпании включает поля: «№ договора», «Дата заключения», «ГК Карс», «Авиакомпания», «Регион», «Вид приложения», «Комментарий», «Файл»; дополнительные соглашения: «№ ДС», «Дата заключения», «Предмет ДС», «Комментарий», «Файл».
- Шаблон создания договора для гостиницы включает поля: «№ договора», «Дата заключения», «ГК Карс», «Гостиница», «Город», «Наименование организации», «Вид услуги», «Отметка о подписании», - «Нормативный акт / форма договора», «Отметка об исполнении», «Комментарий», «Файл»; дополнительные соглашения: «№ ДС», «Дата заключения», «Предмет ДС», «Комментарий», «Файл».
- Фильтры для договоров авиакомпаний при просмотре: «Диапазон дат», «Авиакомпания», «Вид приложения», «ГК Карс».
- Фильтры для договоров гостиниц при просмотре: «Диапазон дат», «Гостиница», «Город», «ГК Карс».

## v10.1 (04.10.2025)
- Страница «Обновления»: публикация иерархических статей (создание/редактирование/просмотр).
- Страница «Инструкции»: документация CRM в древовидной структуре.
- Шахматка: при переходе из «Эскадрильи» выполняется автоскролл к нужной заявке и подсветка.
- Договоры: раздельные разделы для договоров авиакомпаний и гостиниц; в авиакомпаниях возвращён прежний вид карточек.
- Обновлены шаблоны создания договоров (поля, доп. соглашения, вложения).
- Фильтры просмотра договоров: для авиакомпаний — диапазон дат, авиакомпания, вид приложения, ГК «Карс»; для гостиниц — диапазон дат, гостиница, город, ГК «Карс».
- Удаление изображений из галереи и в тарифах.

## v10.2 (15.10.2025)
- Доп. услуги в тарифах гостиниц, название, стоимость, стоимость для авиакомпаний.
- Цены на питание для авиакомпаний.
- Редактирование гостиниц. Добавлена скидка, отображается во вкладке "О гостинице". Видимость гостиницы, наличие питания, самостоятельное размещение перенесены в новую подвкладку "Настройки".

## v10.3 (25.10.2025)
- Добавлены настройки доступа и настройки уведомлений (визуальная часть) во вкладку "Пользователи" авиакомпании. 
- Настройка должностей отдела перенесены из редактирования и создания отдела в настройки доступа. Настройки доступа отдела включает в себя доступ к разделу (эскадрилья, пассажиры, пользователи, сотрудники, об авиакомпании, договоры, отчеты), создание, редактирование. В эскадрильи также есть доступ к чату заявки.
- Удаление фото из галереи перенесено в редактирование.
- Пересекающиеся заявки в шахматке авиакомпании отображаются в два и более ряда.
- Уведомления перенесены в боковую панель и формат дат как в истории.

## v10.4 (21.11.2025)
- Визуальная часть ФАП, получения списка заявок, создание, поставка воды, поставка питания, проживание, трансфер+проживание.
- Визуальная часть аккаунта представительств.
- Плашка "Сайт использует cookies".
- Открытие изображения в модальном окне в инструкциях.

### v10.5 (19.12.2025)
- Доработан раздел «Трансфер» для диспетчера: вкладка «Водители» приведена к единому виду с «Организациями», реализован серверный поиск, возвращена и скорректирована пагинация с учётом новых полей `totalPages` и `totalCount` в запросе `DRIVERS_QUERY`.
- Обновлена логика и отображение списка водителей: для подтверждённых водителей (`APPROVED`) убрана возможность повторного подтверждения/отклонения, реализована подписка на обновления водителей и корректная работа высоты списка без перекрытия пагинации.
- Переработан сайдбар подтверждения водителя (`ConfirmDriver`): добавлены действия «Принять» и «Отклонить» с использованием `UPDATE_DRIVER_MUTATION`, модальное окно указания причины отказа (`refusalReason`), фиксированная высота `textarea`, цвета и `border-radius` приведены к дизайн‑системе.
- Обновлена логика фильтрации в таблице водителей: на странице организаций статус водителя определяется по `organizationConfirmed`, на странице водителей — по `registrationStatus` (`PENDING`, `APPROVED`); при добавлении водителя в организацию учитываются только подтверждённые (`APPROVED`) водители.
- Создан модуль «Договоры» для организаций: добавлен компонент `OrganizationRegisterOfContracts` по аналогии с гостиницами и авиакомпаниями, реализована отдельная вкладка «Договоры» на странице организации с фильтрами по дате, городу, компании и поиском по договорам.
- В списке заявок трансфера добавлен индикатор статуса отчета с обратным отсчётом времени: зелёный таймер при времени до начала более 2 часов или при завершённой заявке, красный таймер при времени менее 2 часов, красный индикатор «Просрочен» при истёкшем времени начала; компонент `ReportTimer` вынесен в отдельный модуль.
- Реализована возможность редактирования пассажиров в заявке трансфера: добавлен выбор и удаление пассажиров через компонент `MultiSelectAutocomplete` в режиме редактирования заявки, обновление списка пассажиров передаётся в мутацию `UPDATE_TRANSFER_REQUEST_MUTATION`.
- Исправлены предупреждения esbuild о дублирующихся ключах в объектах стилей: устранены дубликаты в компонентах `NewPlacement`, `MUITextField`, `DraggableRequest` и `DateRangeModalSelector`.

### v10.6 (22.12.2025)
- Реализована сортировка заявок трансфера по `scheduledPickupAt`: сначала отображаются заявки, до которых осталось менее 2 часов (отсортированные по времени), затем заявки на сегодняшний день, затем остальные заявки (все отсортированы по дате, самые новые вверху).
- Добавлена условная высота списков для режима `disAdmin`: в компонентах `InfoTableDataCompany` и `InfoTableDataDriversCompanies` установлена высота `calc(100vh - 427px)` при `disAdmin === true`, аналогично `InfoTableDataDriversCompany`, для предотвращения перекрытия пагинации.
- В компоненте `ConfirmDriver` реализован просмотр изображений документов в модальном окне: добавлена возможность открытия изображений по клику, увеличения/уменьшения масштаба (от 0.5x до 5x) с помощью кнопок или колесика мыши, перетаскивания увеличенного изображения и скачивания изображений через функцию `handleDownload` с использованием `fetch` для корректного скачивания файлов.

### v10.7 (26.12.2025)
- Реализован ролевой доступ для редактирования договоров: добавлен флаг `canEdit` для ролей `dispatcherAdmin` и `superAdmin`, который контролирует возможность редактирования, создания и удаления договоров авиакомпаний, гостиниц и организаций. Для пользователей без прав редактирования элементы управления скрыты, а высота контента адаптирована.
- Исправлены ошибки работы с массивами: добавлены проверки на существование данных перед использованием методов `.find()` и `.map()` в компонентах редактирования договоров гостиниц и организаций.
- Добавлен стилизованный тултип "Активные заявки" в компоненте `DriverItem`: при наведении на элемент с количеством активных трансферов отображается подсказка.
- Исправлена работа с Yandex Maps: переработана логика выбора адреса в `YandexMapModal` — переход на использование встроенного API `ymaps.geocode` вместо внешнего API, что устранило ошибки 403 Forbidden и проблемы с CORS. Исправлен порядок координат для корректной работы геокодинга, добавлена обработка кликов по предустановленным меткам на карте.
- Создан компонент `TransferMessage` для чатов трансферов: реализован полнофункциональный чат с поддержкой GraphQL подписок для реального времени, отслеживанием непрочитанных сообщений, автоматической прокруткой к непрочитанным сообщениям, эмодзи-пикером и фильтрацией типов чатов по ролям пользователей (для диспетчеров доступны только чаты с водителем и сотрудниками).
- Реализована страница трансферов для `airlineAdmin`: добавлен раздел "Трансфер" в меню авиакомпании, создана страница со списком трансферов, отфильтрованных по `airlineId` пользователя, с возможностью создания новых заявок и просмотра детальной информации о каждой заявке. При создании заявки автоматически устанавливается авиакомпания пользователя, выбор авиакомпании скрыт.
- Добавлена пагинация для трансферов: реализована серверная пагинация с синхронизацией номера страницы с URL, интеграция `ReactPaginate`, сброс пагинации при изменении фильтров. Пагинация работает аналогично реализации в `Estafeta.jsx`.
- Исправлена логика передачи `dispatcherId` при создании трансфера: поле `dispatcherId` передаётся только если пользователь имеет роль `dispatcherAdmin`, для всех остальных ролей (включая `superAdmin` и `airlineAdmin`) передаётся `null`.
- Устранена проблема с рандомным перемещением водителей: удалена функция `shuffleArray` из `InfoTableDataDriversCompany`, водители теперь отображаются в исходном порядке из запроса.
- Обновлена структура роутинга: добавлен маршрут для детальной страницы трансфера (`/orders/:orderId`) в `App.jsx` и `AirlineAdminContent.jsx`, обеспечивающий корректную навигацию к конкретной заявке трансфера.

### v10.8 (17.01.2026)
- Добавлена новая шахматка V2 с модульной структурой: `src/Components/PlacementDNDV2/` (подкаталоги `components/`, `hooks/`, `utils/`).
- Вынесены данные и подписки шахматки в `usePlacementData`, вынесены фильтры/пересечения/позиции в `utils`.
- Добавлен отдельный маршрут для V2: `/newPlacementV2/:idHotel` в `App.jsx`.
- Вкладка шахматки сейчас использует V2 по умолчанию (в `HotelShahmatka_tabComponent.jsx`).
- Исправления UI/поведения V2: ширина карточки со статусом "Ожидает" = 100%, открытие `ExistRequest` по клику, одноразовый скролл к заявке при входе, корректная высота карточки (фиксированная), правки взаимодействия `pointerEvents` в карточке.
- Старую шахматку не удаляли.

### v10.9 (27.01.2026)
- В настройки доступа авиакомпаний добавлен раздел «Трансфер» с правами: доступ к разделу, создание, чат, редактирование.
- Применены права доступа к трансферу в меню и роутинге авиакомпании.
- Ограничены действия в трансфере по правам авиакомпании: создание заявки, открытие чата, редактирование заявки и назначение водителя.

## v11.0 (02.02.2026)
- Реализованы настройки доступа для отделов диспетчеров: создан компонент `DispatcherAccessSettings` с гранулярным управлением правами доступа к разделам системы.
- Настройки доступа диспетчера включают управление правами для следующих разделов: Эскадрилья (доступ к разделу, создание заявки, сообщения в чате, редактирование), Трансфер (доступ к разделу, создание заявки, сообщения в чате, редактирование), Пассажиры (доступ к разделу, создание, редактирование), Пользователи (доступ к разделу, добавление, редактирование), Сотрудники (доступ к разделу, добавление, редактирование), Договоры (доступ к разделу), Аналитика (доступ к разделу), Об авиакомпании (доступ к разделу, редактирование), Отчёты (доступ к разделу, создание).
- Реализованы настройки уведомлений для отделов диспетчеров: создан компонент `DispatcherNotificationsSettings` с возможностью настройки уведомлений на почту и в браузере.
- Настройки уведомлений диспетчера включают: уведомления о заявках (создание заявки, изменение дат заявки, смена размещения заявки, отмена заявки), уведомления о бронях (создание брони, запрос на изменение дат брони, обновление брони, смена размещения брони), уведомления о сообщениях (новое сообщение в чате).
- Добавлена сегментированная навигация между вкладками «Уведомления» и «Доступ» в настройках отделов диспетчеров.
- Обновлена логика компонента `NotificationsSettings` для авиакомпаний: добавлено определение правильного маршрута на основе роли пользователя при переходе между вкладками уведомлений и доступа.

### v11.1 (18.02.2026)
- Переработан интерфейс панели уведомлений в `NotificationsPermissionsPanel`: строка настроек разделена на колонки — текст, MUISwitch (выровнены в единую вертикальную колонку), чекбокс «на почту», чекбокс «в браузере».
- MUISwitch вынесен в отдельную колонку фиксированной ширины для единообразного выравнивания по всем строкам.
- Чекбоксы «на почту» и «в браузере» выровнены под соответствующими заголовками колонок.
- Реализованы фильтры по городу, оценке и звёздности в списке гостиниц (в запросе, по аналогии с договорами). Фильтр по городу использует cityId через связь с договорами; список городов — из GET_CITIES.
- При статусах заявки «создана» и «в обработке» блок добавления сотрудника (синий +) всегда доступен; при добавлении нового сотрудника он автоматически выбирается в автокомплите.
- Если сотрудник уже назначен при статусах «создана» или «в обработке», добавить или изменить сотрудника нельзя; редактирование доступно после размещения.
- Блок «Изменение даты» скрыт при статусах «создана» и «в обработке»; даты редактируются в полях заезд и выезд.
- При редактировании заявки скрываются вкладки «Комментарии» и «История».
- Исправлена ошибка Prisma при обновлении заявки: personId и hotelId обрабатываются через связи.

### v11.2 (19.02.2026)
- Разделены разделы «Трансфер» и «Автопарк»: в «Трансфере» отображаются только заказы, в «Автопарке» — организации и водители с иконкой автобуса.
- Создан компонент `DisAdminAutoparkContent` с вкладками «Организации» и «Водители»; `DisAdminTransferContent` упрощён — только заказы.
- Добавлен пункт меню «Автопарк» с иконкой автобуса в `DisAdminMenu` и `SuperAdminMenu` (ссылка на `/driversCompany`).
- Исправлено обрезание лейблов MUI TextField в фильтрах и поиске раздела «Трансфер» и «Автопарк»: для родительских контейнеров установлен `overflow: visible` в `DisAdminTransferContent`, `DisAdminAutoparkContent`, `TransferOrders`, `DriversCompanyList`, `DriversList`.
- Обновлён `SuperAdminMenu` по аналогии с `DisAdminMenu`: синхронизированы структура, стили и порядок пунктов, добавлены `svgWrapper`, разделитель и нижний блок с версией и ссылкой на Alazar studio.

### v11.3 (20.02.2026)
- Реализована логика заявок трансфера по аналогии с эскадрильей: при клике на заявку открывается боковая панель `ExistRequestTransfer` вместо перехода на карту.
- Создан компонент `ExistRequestTransfer` с вкладками «Общая» и «Комментарии», отображением данных заявки, блока «Информация о водителе» (организация, ФИО, машина, гос. номер, рейтинг); кнопка «Перейти на карту»; закрытие панели по клику вне неё (без overlay).
- Приведён к единому виду таблица заявок трансфера (`InfoTableDataTransferOrders`): используются стили из `InfoTableData.module.css`, структура соответствует `InfoTableData`.
- Структура таблицы трансферов: 7 колонок — № (с индикатором ReportTimer), Пассажиры, Дата и время, Авиакомпания, Подача, Пункт назначения, Статус; убрана иконка чата из таблицы.
- В номерном фонде гостиницы (`HotelNomerFond_tabComponent`) фильтры «Квота» и «Резерв» реализованы через `MUIAutocomplete` на уровне компонента.
- В `InfoTableDataNomerFond` удалены кнопки «Квота»/«Резерв», логика фильтрации перенесена на родительский компонент.
- Реализован отдельный доступ к автопарку (организациям) для диспетчеров: добавлены права `organizationMenu`, `organizationCreate`, `organizationUpdate`, `organizationAddDrivers`, `organizationAcceptDrivers`. Настройки доступны в `DispatcherAccessSettings`; видимость раздела, создание организаций, редактирование, добавление и приём водителей управляются по правам. Пункт меню «Автопарк» и роутинг проверяют `organizationMenu`.
- Расширены права для реестра договоров: добавлены `contractCreate` и `contractUpdate`. В настройках доступа диспетчеров можно включать/отключать создание и редактирование договоров (включая удаление и доп. соглашения). Права применяются в `RegisterOfContracts`, `HotelRegisterOfContracts`, `AirlineRegisterOfContracts`, `OrganizationRegisterOfContracts`.
- Унифицирована структура меню: `HotelAdminMenu` и `AirlineAdminMenu` приведены к структуре `DisAdminMenu`.

### v11.4 (21.02.2026)
- Переработан формат сообщений в чатах (`Message`, `SupportMessage`, `TransferMessage`): добавлены разделители дат по центру над группами сообщений дня.
- Входящие сообщения: аватар, имя и должность отправителя отображаются только у первого сообщения в серии от одного отправителя; аватар, данные собеседника и текст объединены в общий контейнер с единым фоном.
- Последующие сообщения от того же отправителя выводятся без левого отступа (spacer).
- Текст первого сообщения прижат к левому краю (под аватаром), время размещено в правом нижнем углу общего блока.
- Аватар загружается из `sender.images[0]` при наличии, иначе используется `/no-avatar.png`.

### v11.5 (25.02.2026)
- В ФАП у каждого отеля добавлен массив людей (брони); на странице детали гостиницы заявки реализовано добавление и удаление броней.
- В запросе `GET_PASSENGER_REQUEST` в блоке отелей проживания добавлена выборка `people`; добавлены мутации `ADD_PASSENGER_REQUEST_HOTEL_PERSON` и `REMOVE_PASSENGER_REQUEST_HOTEL_PERSON`.
- Создан компонент `AddRepresentativeBooking`: модальное окно «Добавить бронь» с полями ФИО (обязательно), телефон, пол, номер комнаты; валидация по ФИО, уведомления об успехе и ошибке, Dialog с `border-radius: 15px`.
- На странице гостиницы ФАП таблица броней заполняется из API (колонки: ID, дата/время заезда, дата/время выезда, номер комнаты, ФИО); кнопка «Добавить бронь» открывает модалку; удаление брони по иконке с подтверждением через `DeleteComponent`.

### v11.6 (26.02.2026)
- Реализован компонент `UserActivityTracker`: отслеживание неактивности пользователя (5 минут) по событиям мыши, клавиатуры и скролла.
- При истечении 5 минут без активности показывается модальное окно «Вы тут?» и сразу отправляется запрос `markUserOffline` (токен передаётся в заголовке Authorization).
- При первой активности после перехода в офлайн отправляется запрос `markUserOnline`; модалка при этом остаётся открытой.
- Модалка закрывается только при явном действии пользователя: нажатие кнопки «Я тут» или клик вне модального окна; при закрытии сбрасывается таймер неактивности.
- Трекер подключён в `App.jsx` и активен только для авторизованных пользователей.

### v11.7 (25.02.2026)
- В отчёте по отелю (ФАП) итоговая стоимость считается только по полям «Ст-ть питания» и «Ст-ть проживания».
- При нажатии «Сформировать отчёт» данные таблицы сохраняются на бэкенде (модель `PassengerRequestHotelReport`, мутация `savePassengerRequestHotelReport`); при открытии страницы отчёта подставляются сохранённые данные.
- Для поля «Суток» в таблице отчёта по отелю добавлен шаг 0,5 (`step={0.5}`).
- Кнопки «Вода доставлена» и «Питание доставлено» переводят статус услуги в ACCEPTED; IN_PROGRESS выставляется на бэкенде при добавлении человека (`addPassengerRequestPerson`), COMPLETED — когда количество добавленных людей достигает планового.
- В форме «Добавить гостиницу» (проживание ФАП) добавлена возможность быстро создать гостиницу: ссылка «Нет в списке? Создать гостиницу», форма с полями «Название» и «Город» (выбор из GET_CITIES); после создания гостиница подставляется в форму, адрес в быстром создании не запрашивается.

### v11.8 (02.03.2026)
- В реестре договоров интерфейс редактирования договоров и дополнительных соглашений приведён к единому виду с редактированием заявки.
- Внешний вид блоков «История» и «Уведомления» приведён в соответствие с дизайном.

### v11.9 (04.03.2026)
- Введён общий компонент дополнительного меню «Редактировать / Удалить»: создан `Standart/AdditionalMenu`, пять экранов переведены на него (EditRequestAirlineCompany, ExistRequestCompanyHotel, ExistRequestCompany, SettingsSidebar, EditRequestDispatcherDepartment); удалены дубликаты компонентов и стилей.
- В форме редактирования отдела авиакомпании (EditRequestAirlineOtdel) добавлено дополнительное меню в шапку; режим редактирования включается через пункт «Редактировать»; кнопки «Отмена» и «Сохранить» отображаются только в режиме редактирования.
- В форме редактирования компании (ExistRequestMyCompany) добавлено дополнительное меню в шапку; кнопки «Отмена» и «Сохранить» отображаются только в режиме редактирования; при отмене форма восстанавливается из загруженных данных компании; пункт «Удалить» в меню не отображается.
- Профиль пользователя переведён с выпадающей модалки на боковую панель: по клику на аватар в шапке открывается правая боковая панель «Профиль» (компонент `ProfileSidebar`).
- В панели профиля: блок с аватаром, ФИО и должностью; пункты меню с иконками и подзаголовками — «Мой профиль» (ФИО, аватар), «Вход и безопасность» (пароли, телефон, e-mail), «Уведомления» (на почту, в браузере), «Помощь» (инструкции по работе в системе), «Техподдержка» (написать в техподдержку); переключатель темы оформления (Светлая / Микс / Темная) с сохранением выбора в `localStorage` и применением атрибута `data-theme` к документу; кнопка «Выход».
- Пункт «Помощь» выполняет переход на страницу документации по маршруту `/documentation`.
- Пункты «Мой профиль» и «Вход и безопасность» открывают существующий сайдбар редактирования профиля (`ExistRequestProfile`); «Уведомления» — панель уведомлений; «Техподдержка» — сайдбар техподдержки.
- В `Header` удалены выпадающий блок профиля (`profile_dropdown`), связанные состояния и неиспользуемые стили в `Header.module.css`.

### v11.10 (25.02.2026)
- Реализована история (лог) заявки ФАП по аналогии с гостиницей и авиакомпанией: на бэкенде в модель `Log` добавлено поле `passengerRequestId` и связь с `PassengerRequest`; все действия по заявке ФАП (создание, обновление, смена статусов, добавление отелей и пассажиров, переселение, выселение, досрочное завершение, сохранение отчёта и т.д.) записываются в лог с привязкой к заявке.
- В GraphQL для типа `PassengerRequest` добавлено поле `logs(pagination: LogPaginationInput): LogConnection!`; резолвер возвращает список логов с пагинацией и сортировкой по дате (сначала новые).
- На странице заявки ФАП (ReservePlacementRepresentative) добавлена кнопка «История»; по нажатию открывается модальное окно с историей заявки: записи сгруппированы по дням, для каждой записи отображаются время, описание действия и пользователь (аватар, имя, роль); реализована пагинация по 20 записей.
- Создан отдельный компонент `PassengerRequestLogs` (`src/Components/Blocks/LogsHistory/PassengerRequestLogs.jsx`) по образцу `Logs.jsx`: принимает пропсы `show`, `onClose`, `passengerRequestId`; внутри — запрос `GET_PASSENGER_REQUEST_LOGS`, группировка по дням, пагинация (по 50 записей), отображение времени, описания, причины (если есть) и аватара пользователя.
- Стили истории ФАП приведены к виду `Logs.module.css`: используются те же классы и вёрстка (`requestTitle`, `requestData`, `logs`, `historySection`, `historyDate`, `logText`, `logImg`, `historyLogTime`, `historyLog`, пагинация с `activePaginationNumber`, `paginationNumber`); добавлен класс `historyLogReason` для блока «Причина»; при загрузке отображается `MUILoader`.
- На странице представителя (ReservePlacementRepresentative) логика и разметка истории вынесены в `PassengerRequestLogs`; подключение через `<PassengerRequestLogs show={showHistoryPanel} onClose={...} passengerRequestId={idRequest} />`.
- В таблице броней на странице гостиницы ФАП (`RepresentativeHotelDetail`) убрана колонка «Категория / вид»; остаются ID, дата/время заезда и выезда, номер комнаты, ФИО (плюс чекбокс и кнопки действий).
- В форме добавления и редактирования брони (`AddRepresentativeBooking`) убраны поля «Категория номера» и «Вид номера»; остаются ФИО пассажира, номер телефона, номер комнаты. Категория и вид задаются только в отчёте по отелю.
- На странице отчёта по отелю (`RepresentativeHotelReportPage`) при добавлении нового гостя в брони он появляется в отчёте: при загрузке сохранённые строки объединяются с гостями из текущего списка броней (новые гости добавляются в отчёт).
- Отчёт по отелю переведён на модель «группы»: задаются категория и вид номера и общие поля (завтрак, обед, ужин, ст-ть питания, ст-ть проживания), к группе привязываются гости только из уже добавленных броней; сутки вычисляются по дате/времени заезда и выезда из брони (с возможностью редактирования); добавлены табы «Группы» и «Таблица» (стили табов как в проекте — `filter_wrapper`, `activeButton`); выгрузка в Excel в прежнем формате (одна строка на человека).

### v11.11 (10.03.2026)
- Реализованы цены на трансфер (TransferPrices) для организаций: добавлена вкладка «Цены» на странице организации (между «Реестр договоров» и «Об организации»).
- Список цен: аккордеон по образцу тарифов авиакомпании — поиск по ценникам, кнопка «Добавить ценник», карточки с раскрытием (вместимость 3/5/7-местный, межгород/город в виде плашек, блоки «Аэропорты» и «Города»), иконка редактирования у каждой записи.
- Добавление и редактирование в правой выезжающей панели: форма без обёртки `<form>` (как в авиакомпании), поля по умолчанию 0; заголовки «Добавить ценник» / «Изменить ценник», шесть числовых полей (межгород/город для 3, 5 и 7-местного), мультиселекты «Аэропорты» и «Города»; в режиме редактирования — сначала только просмотр, по кнопке «Изменить» включается редактирование, кнопки «Отмена» и «Сохранить» с иконками; при закрытии панели — подтверждение при несохранённых изменениях.
- GraphQL: в запрос `GET_ORGANIZATION` добавлен фрагмент `TransferPriceFields` и поле `transferPrices`; при сохранении в мутацию `UPDATE_ORGANIZATION` передаётся `input.transferPrices` (массив без `__typename`).
- Утилиты: `src/utils/transferPrices.js` — константа `DEFAULT_TRANSFER_PRICES` (все цены 0), `createEmptyTransferPriceInput`, `transferPriceToInput`, `transferPriceInputsToPayload`; переиспользуемый компонент `TransferPricesForm` и сайдбар `TransferPriceSidebarForm`; список — `InfoTableOrganizationTransferPrices`.
- В модалках «Выселить пассажира» и «Переселить пассажира» (страница гостиницы ФАП, `RepresentativeHotelDetail`) поле причины переведено на обычный `<textarea>` (класс `reasonTextarea`); в модалке «Переселить пассажира» выбор отеля выполняется через компонент `MUIAutocompleteColor` вместо `<select>`.
- Кнопки «История» и «Добавить услугу» на странице заявки ФАП перенесены на один уровень с табами (в один ряд с «Проживание»); серая граница под строкой отображается только под табами, не под этими кнопками (`tabsRowWrapper`, без полной линии под `tabsRowActions`).
- В блоке «Проживание» (ФАП) рядом с заголовком «Ссылка» в шапке таблицы добавлено отображение количества людей по услуге: иконка и число из `request.livingService.plan.peopleCount` (как в трансфере, питании и доставке багажа), компонент `HabitationTab`.
- При добавлении гостиницы в ФАП (`AddRepresentativeHotel`) учтено ограничение по количеству мест на услугу: поле «Количество мест» не может превышать остаток (`livingService.plan.peopleCount` минус сумма мест по уже добавленным гостиницам); при вводе значение ограничивается остатком, выводятся уведомления при превышении; под полем — подсказка «Для услуги проживания указано N мест, уже распределено M, осталось K»; при отправке формы — проверка и понятные сообщения об ошибке.
- При нажатии «Переселить» в детали гостиницы ФАП, если в заявке только одна гостиница, модалка не открывается, вместо этого показывается уведомление: «Переселение недоступно: в заявке только одна гостиница. Добавьте ещё одну гостиницу, чтобы переселять пассажиров.» (`addNotification`, тип `info`).
- Расчёт суток в отчёте по отелю переведён на эффективные сутки с учётом частичных (0,5 и 1): добавлена утилита `src/utils/effectiveCostDays.js` — функция `calculateEffectiveCostDays(arrival, departure)` — базовые сутки как полные календарные дни между датами заезда/выезда; заезд до 06:00 → +1 сутки, до 14:00 → +0,5 суток, 00:10 → 0; выезд 23:50 или не ранее 18:00 → +1 сутки, после 12:00 → +0,5 суток. В `RepresentativeHotelReportPage` сутки по каждому гостю считаются через эту функцию (по датам из `accommodationChesses` или по плану услуги).
- Выбор всех элементов в `MuitiSelectAutoComplete`.

### v12.0 (20.03.2026)
- Раздел «Аналитика» (вкладка авиакомпаний) переведён на новый GraphQL-контракт: добавлены запросы `cityRegions` и `analyticsAirlineServiceComparison`; данные отображаются по связке `region × service` с блоками `period1`, `period2` и `diff`.
- В аналитике авиакомпаний добавлены фильтры по услугам, составу и регионам, а также сравнение двух периодов (`Период 1` / `Период 2`) с сохранением текущей логики выбора авиакомпании.
- Добавлены значения по умолчанию для стартового запроса аналитики авиакомпаний: услуга `LIVING`, состав `SQUADRON`, регионы — первые доступные из `cityRegions` (чтобы не выполнять «поиск всего по всему»).
- Визуализация аналитики авиакомпаний расширена с использованием `AnalyticsChart`: добавлены графики по бюджету `P2` по услугам, топу регионов по людям (`P2`) и сравнительный график по периодам; табличная детализация сохранена.
- Исправлено отображение осей/подсказок в `AnalyticsChart`: для категориальных значений больше не применяется форматирование как даты (устранены `Invalid Date` и `NaN` в столбчатых графиках).
- Фильтры авиакомпаний в шапке аналитики переведены на компоненты `MUIAutocomplete` и `MultiSelectAutocomplete`; фильтры и кнопки периодов выровнены на одном уровне с заголовком блока.
- Доработан `MultiSelectAutocomplete`: ограничение ширины тегов применено через `.MuiAutocomplete-tag`; поведение ввода сделано комбинированным — в неактивном состоянии отображение в одну строку, в активном состоянии возвращается стандартное поведение MUI для поиска и редактирования.

### v12.1 (06.04.2026)
- Стартовая страница «Обновления» переработана на базе общего движка документации: `UpdatesList` стал тонкой обёрткой над `DocumentationList` — те же фильтры, дерево разделов и просмотр в Tiptap; `DocumentationList` принимает пропсы `documentationType` и `title`.
- В заявку на пассажирские услуги добавлена кнопка копирования PWA-ссылки (`representativeLinks` в GraphQL); внешний вход (`ExternalLogin`) теперь поддерживает deep-link с `passengerRequestId` для скоупа `REPRESENTATIVE`.
- В панели уведомлений (`NotificationsPermissionsPanel`) для каждого события добавлены отдельные каналы доставки — «на почту» и «в браузере» (`email`, `sitePush`).
- Все ключи уведомлений `reserve*` переименованы в `passengerRequest*` (`reserveCreate` → `passengerRequestCreate` и т.д.) в `NotificationsPermissionsPanel`, `DispatcherNotificationsSettings`, `NotificationsSettings`, `SettingsSidebar`; заголовок секции «Брони» → «ФАП».
- Сайдбар уведомлений: фильтры теперь работают на серверном `type` (`request` / `passengerRequest`), добавлен дедуп при пагинации; «Перейти к заявке» для пассажирских уведомлений ведёт на `/representativeRequests/...`; клик в header-уведомлении тоже различает `passengerRequestId`.
- Введены новые глобальные UI-примитивы: `MUIAlert`, `MUIConfirm`, `ToastContext` (`useToast`), `DialogContext` (`useDialog`); подключены провайдеры в `main.jsx`; нативные `window.alert` / `window.confirm` начали поэтапно заменяться.
- Раскатка `useToast` / `useDialog` по всем основным модулям: заявки и эскадрилья, трансфер, представительские (Hotel/Driver/Baggage), компании/договоры/отчёты/водители, тарифы авиакомпании и гостиницы, доп. услуги, номерной фонд; локальные `Notification`-стейты удалены из `AirlinesList`, `HotelsList`, `TransferOrders`, `RepresentativeRequests` и др.
- В модалке неактивности (`UserActivityTracker`) переработан внешний вид: MUI Stack-раскладка, иконка часов, фраза «За последние N минут» с правильным склонением, кнопка «Продолжить работу».
- Внешние пользователи в заявке ФАП: меню скрывается при `isExternalPassengerRequestUser`; `Header` принимает проп `isExternalUser` на страницах отчёта по гостинице, водителя и детали гостиницы.
- В вкладках услуг (`HabitationTab`, `PowerSupplyTab`, `WaterSupplyTab`) строки таблицы обёрнуты в `.tableBody`-контейнеры для корректной работы sticky-заголовков и скролла.
- При отправке отчёта по гостинице ФАП показывается toast «Отчёт сформирован».
- `auth_fingerprint` перенесён из `sessionStorage` в cookie — теперь сохраняется между вкладками и работает в SSR-сценариях.
- В `CreateRequestDispatcherCompany` добавлен инлайн-создатель должности «+ Должность» (раньше нужно было открывать отдельный сайдбар).
- На странице сотрудников авиакомпании появилась синтетическая группа «Без отдела» — пользователи без подразделения больше не теряются; список обновляется после удаления.
- В тарифах авиакомпании пустые поля цен передаются как `null` вместо `0`, чтобы корректно отображались плейсхолдеры.
- В заявке (`ExistRequest`) добавлен защитный гард: разместить заявку можно только после подтверждения выбранного сотрудника; пункт «Добавить сотрудника» переименован в «Подтвердите выбор сотрудника».
- В заявке трансфера (`ExistRequestTransfer`) добавлено выпадающее меню «...» с пунктами «Перейти на карту» и «Отменить заявку».
- `ConfirmDriver`: `extraEquipment` теперь массив, фото берётся по индексу `driverPhoto[0]`.
- В номерах гостиницы (`HotelAboutRoomBlock`) Swiper переведён в режим `loop` с постоянно активными стрелками.
- В Эстафете отключён фронтенд-поиск (вся фильтрация на сервере); в шапке аналитики авиакомпаний выправлена раскладка фильтров (`flexWrap`, ширины).

### v12.2 (14.04.2026)
- Аналитика авиакомпаний переведена на новый GraphQL-эндпоинт `airlineAnalytics`: единый запрос возвращает `airportsBreakdown`, `positionsBreakdown`, `segments`, `summary`; добавлены режимы группировки (без группировки, по аэропорту, по должности, по периоду) и переключатель метрики столбчатого графика (траты / заявки / люди / номера).
- Аналитика расширена сравнением периодов: ответ разбит на блоки `period1` / `period2`, добавлен `servicesBreakdown` и список заявок (`requests`); в `AnalyticsChart` появился тип `groupedBar` для многосерийных столбцов с кастомной легендой; автоматическая адаптация ширины столбцов.
- Аналитика, вкладки по услугам (`LIVING` / `MEAL` / `TRANSFER`): сортируемые таблицы заявок и трансферов; данные нормализуются через `airlineAnalyticsMappers.js` и `analyticsTableSortUtils.js`; для разработки добавлены mock-данные.
- Экспорт аналитики в PDF: разбиение на страницы по атрибутам `data-pdf-section` через `html2canvas` + `jspdf`, длинные канвасы разрезаются с полями и отступами, отображается прогресс экспорта (`{current, total}`); добавлены зависимости `html2canvas`, `html2pdf.js`, `jspdf`.
- Фильтр периода в аналитике перенесён в модальное окно (кнопка «Фильтры» в строке вкладок), активный период отображается бейджем над вкладками («Период 1 ... Период 2 ...»); рядом с заголовком — инлайн-выпадающий список «Услуги» для быстрой фильтрации без переоткрытия фильтра.
- Аналитика авиакомпаний больше не выбирает первую авиакомпанию автоматически: при отсутствии выбора отображается empty-state «Выберите авиакомпанию» с трёхшаговой инструкцией и SVG-иллюстрацией.
- В `CreateRequestReport` сообщение об отсутствии цен уточнено: «У авиакомпании нет цен по выбранному аэропорту».
- `DateRangePickerCustom` больше не подставляет «последние 7 дней» по умолчанию: используется сегодняшний день, перед использованием значения проверяется его полнота.
- Добавлен модуль Script Runner (только для `SUPERADMIN`): открывается из иконки в шапке, состояние сохраняется в `localStorage`; записывает и воспроизводит интерактивные сценарии — клики, drag-and-drop, выбор в автокомплите, заполнение форм, навигацию; в `MUIAutocomplete`, `MUIAutocompleteColor`, `MultiSelectAutocomplete` добавлены хуки `data-script-runner-id`.
- Script Runner: экспорт и импорт всей библиотеки или отдельного скрипта в JSON, автопереименование при конфликтах, уведомления об успехе/ошибке.
- Финальная миграция глобальных уведомлений: оставшиеся `window.alert` / `window.confirm` / самописный `Notification` заменены на `useDialog` / `useToast` (`Header`, сайдбар профиля, настройки гостиницы/авиакомпании, Patch Notes, модалка неактивности); в окне неактивности появилась кнопка «Выйти»; z-index `MUIAlert` / `MUIConfirm` поднят над сайдбарами; ссылка на Patch Notes вернулась в левое меню с указанием версии «ver 3.1.1».
- В аналитике авиакомпании переработаны круговые/кольцевые диаграммы: легенда вынесена из Recharts в кастомный отсортированный список снаружи, исправлена пропорциональность контейнера, обновлена иконка пустого состояния.
- Дополнительное меню строк (`AdditionalMenu` — «Редактировать / Удалить») больше не уходит под боковые панели: z-index поднят до 10050.
- В сайдбаре создания отчёта (`CreateRequestReport`) переработан жизненный цикл формы: полный сброс при открытии, разные правила валидации для отчёта по гостинице и по авиакомпании, обновлён `MUIConfirm`.
- Во вкладках авиакомпании (Компания, Договоры, Тарифы, Цены на трансфер, Номерной фонд, Подразделения) клик по строке открывает сайдбар в режиме просмотра, иконка карандаша — сразу в режиме редактирования (новый проп `initialEditMode` во всех `EditRequest*`-сайдбарах).
- Страница «Обновления» (Patch Notes): обновлён внешний вид сайдбаров создания и редактирования, карточек списка — отступы, шрифты, границы шапки/подвала, кнопка закрытия; z-index сайдбара поднят.

## v12.3 (22.04.2026)
- Запущен ФАП v2 — новый интерфейс заявок на пассажирские услуги: список с поиском и фильтром по статусу, страница детали с услугами по секциям (Проживание, Трансфер, Вода/Питание, Багаж), переходы по статусам (`CREATED → ACCEPTED → IN_PROGRESS → COMPLETED`), страница отчёта по гостинице; раздел добавлен в меню `SUPERADMIN`, `DISPATCHERADMIN`, `AIRLINEADMIN` и представителей.
- Гостиничный отчёт ФАП переведён на модель «тарифов»: именованные тарифы с полями `breakfast`, `lunch`, `dinner`, `foodCost`, `accommodationCost` привязываются к каждому гостю; вкладки «Тарифы» / «Гости»; восстановление по сохранённым строкам отчёта; автоматический пересчёт суток через `calculateEffectiveCostDays`.
- Страница детали заявки ФАП: подключён общий `Header`, отмена заявки вынесена в MUI-диалог, добавлены боковые панели логов (`PassengerRequestLogs`) и чата (`Message`); кнопка «+ Услуга» скрывается, когда все услуги уже добавлены.
- В секции «Проживание» ФАП добавлено модальное окно управления гостями отеля (`HotelGuestsModal`): добавление, редактирование, удаление, переселение между отелями и досрочное выселение с указанием причины; поиск, мультиселект; кнопка «Сформировать отчёт»; секция стала управляемым аккордеоном (`isOpen` / `onToggle`).
- Страница «Инструкции»: убрана зона «Перезагрузить статьи» из правого заголовка, уменьшен отступ дерева слева, тоньше активная подсветка пункта.
- В ФАП v2 добавлена отдельная страница услуги (`FapServicePage`): каждая услуга открывается на полноэкранной странице с собственным маршрутом `/fapv2/:requestId/service/:serviceKey`; страница заявки переработана в навигационный экран с карточками-ссылками на услуги.
- В фильтры списка заявок ФАП v2 добавлены фильтр по авиакомпании и фильтр по аэропорту; статус фильтра сохраняется в `localStorage`.
- Убрана возможность ручного добавления человека в услугах «Поставка воды» и «Поставка питания» — в соответствии с логикой старого ФАП; формат отображения времени выдачи приведён к `HH:mm` (только время).
- Логика ссылок во всех услугах приведена к старому ФАП: для отелей — кнопки «CRM» / «PWA» / «Ссылка» копируют соответствующее поле в буфер; для водителей трансфера и багажа — кнопка «PWA» или «Ссылка»; диалог генерации ссылок удалён из `FapLivingSection`.
- Во всех секциях услуг кнопки действий перенесены в `topRow` — на один уровень с плановой информацией, выровнены вправо; убраны отдельные блоки кнопок под таблицей.
- Прогресс-бар заполнения добавлен в услуги «Поставка воды», «Поставка питания» и «Трансфер» по аналогии с «Проживанием»; общие классы `progressBar` / `progressFill` вынесены в `FapDetail.module.css`.
- Ширина модалки добавления брони в «Проживании» сделана адаптивной: `maxWidth="sm"` для формы, `maxWidth="md"` для списка гостей.

### v12.4 (30.04.2026)
- Полировка ФАП v2: добавлен общий компонент `FapDestructiveModal` для деструктивных действий (отмена, завершение), упрощены секции «Багаж», «Проживание», «Трансфер», «Вода/Питание», улучшена раскладка модалки `YandexMapModal`.
- Добавлен прототип PMS-системы для гостиниц (`HotelPMS`) на моковых данных: боковая навигация, дашборд, PMS-шахматка с динамической шириной дня и выбором месяца/года, брони, номерной фонд, эксплуатация, тарифы, отчёты; маршруты подключены в роутер, добавлены пункты меню. PMS-таймлайн стартует за 7 дней до сегодняшнего дня.
- Сайдбар существующей заявки переработан для статусов «создан» и «в обработке»: даты отображаются как read-only поля (редактирование только в режиме Edit); в `AdditionalMenu` добавлен пункт «Разместить» с иконкой `UserCheckIcon`; отдельный ряд кнопок «Отмена / Разместить» убран.
- Панель настройки доступа (`AccessPermissionsPanel`) переработана: вместо отдельных переключателей внутри каждого раздела теперь главный переключатель «Взаимодействие с разделом», который включает/выключает все подфлаги (создание, редактирование, чат и т.д.) сразу.
- Из настроек доступа удалена отдельная таблица должностей; вместо неё внутри карточек «Эскадрилья», «Трансфер» и «Сотрудники» встроен мультиселект должностей.
- Сайдбар трансфера (`OrderInfoSidebar`) переработан: добавлены вкладки, интеграция чата `TransferMessage`, меню отмены; в `TransferOrder` добавлены подтверждение и чат.
- В разделах представительских услуг (`RepresentativeHotelDetail`, `HabitationTab`, `BaggageDeliveryTab`, `WaterSupplyTab`, `PowerSupplyTab`, `TransferAccommodationTab`) добавлен режим только чтения для роли авиакомпании: скрыты кнопки добавления, редактирования, удаления и ссылок; «Сформировать отчёт» переименовано в «Отчёт».
- Уточнены права доступа в карточках заявок и сайдбарах: скрытие меню редактирования диспетчера при отсутствии `userUpdate`, гейтинг редактирования тарифов и стоимости в `FapReport` через `canEdit`, запрет назначения водителя для роли авиакомпании в `TransferOrder`, проверка `transferUpdate` перед отменой трансфера.
- Документация: порядок дерева сохраняется отдельно для каждого пользователя в `localStorage` (`documentationTreeOrder.js`).

### v12.5 (12.05.2026)
- В услугах ФАП v2 (гостиница, водитель, водитель багажа) добавлена возможность удаления элементов; редактирование сотрудника авиакомпании (`UpdateRequestAirlineStaff`) переведено на паттерн view/edit с `AdditionalMenu`.
- Экспорт аналитики авиакомпании переработан на ExcelJS + html2canvas: в файл встраиваются графики (столбчатые, круговые) вместе с табличными листами «Сводка», «Аэропорты», «Должности», «Заявки», «Трансфер».
- Для роли авиакомпании скрыты колонки со ссылками (`PWA` / `CRM`) в табах размещения, трансфера и доставки багажа, а также в секциях ФАП v2 (флаги `showLinks` / readOnly).
- Настройки доступа диспетчера (`DispatcherAccessSettings`) переведены на общий компонент `AccessPermissionsPanel` из `SettingsSidebar`.
- Раздел «Авиакомпании» в меню диспетчера и соответствующий маршрут закрыты по флагу `airlineMenu`; кнопка «Создать заявку» в пассажирских заявках закрыта по `reserveCreate`.
- В списке пассажирских заявок и в фильтре статусов заменены захардкоженные ярлыки на общий `REQUEST_STATUS_CONFIG` (цветные бейджи); добавлен полный набор статусов `CREATED`, `ACCEPTED`, `IN_PROGRESS`, `COMPLETED`, `CANCELLED`.
- Защита от двойной отправки: кнопка «Создать заявку» блокируется на время выполнения мутации.
- В меню диспетчера под логотипом добавлен лейбл «beta» с адаптивным отображением по ширине экрана.
- В модалке выбора адреса (`YandexMapModal`) добавлено поле поиска с подсказками от Yandex Geocoder.
- Apollo error link: исправлено двойное проксирование ошибок, не связанных с авторизацией.
- Ребрендинг: новый логотип `kars_avia_logo` (вместо «CRM Kars Avia»), обновлены favicon и набор иконок.
- При создании заявки время заезда и выезда сделано необязательным: по умолчанию подставляется `14:00` и `12:00`; в `ChooseHotel` выводится предупреждение и кнопка «Вернуться к заявке», открывающая карточку в режиме редактирования.
- В карточку заявки добавлено редактируемое поле «Примечание».
- На карточку размещённой заявки добавлена вкладка «Цены»: расчёт стоимости проживания и питания за эффективные сутки по тарифам авиакомпании и гостиницы; при создании заявки доступен предпросмотр цен гостиниц.

### v12.6 (18.05.2026)
- Поддержка (`SupportPage`): тикеты теперь показывают роль, организацию и должность отправителя рядом с именем; счётчик непрочитанных отображает число; добавлена колонка «Последнее сообщение» с датой и временем; статус автоматически выводится как «Нет обращения» / «Открыт» по сообщениям; добавлены вкладки «Активные» / «Архивные»; оптимистичная отправка сообщений; кнопка «Взять в работу» заменяет поле ввода до момента закрепления тикета.
- Добавлена страница подтверждения e-mail (`/verify-email`): принимает токен из письма, вызывает мутацию `VERIFY_EMAIL`, показывает успех/ошибку; на форме входа отображается сообщение «Подтвердите email перед входом» при соответствующей ошибке от бэкенда.
- Первая версия интеграции с TravelLine: новый раздел админки `TravellinePage` с вкладками «Настройки», «Все гостиницы», «Поиск и бронирование», «Консоль»; сайдбар `TravellineRoomsSidebar` для бронирования внешних номеров; модалки `PropertyModal` и `ReservationDetailModal`.
- `ChooseHotel` переведён на унифицированный запрос `hotelOptionsForPlacement` (локальные гостиницы и TravelLine в одном списке) с проверкой доступности TravelLine на даты заявки; в форме редактирования заявки добавлен бейдж «TravelLine» и номер внешней брони, если источник — travelline.
- TravelLine: диалог отмены `TlCancelDialog` с расчётом штрафа за отмену (`TL_CANCELLATION_PENALTY`) перед подтверждением; модалка `SyncProgressModal` с прогрессом «загружено N из M гостиниц»; во вкладке «Настройки» — секция «Каталог отелей» с ручной пересинхронизацией и настраиваемым интервалом автосинхронизации (1–168 ч).
- nginx: добавлен прокси `/uploads/` для загрузки фотографий гостиниц с бэкенда.
- `ChooseHotel` временно скрывает варианты TravelLine (отображаются только локальные гостиницы); WebSocket вернулся к `wss://`.
- В шахматке V2 сопоставление заявок и резерва с гостиницей переведено на `airport.id` вместо текстового `information.city` (устранены пропуски и дубликаты при разном написании городов).
- Реестр договоров: новые поля `contractEndDate` (дата окончания) и `isProlongationEnabled` (возможность пролонгации) для договоров авиакомпаний, гостиниц и организаций; в списке отображается срок действия с подсветкой «Истёк» / «Истекает»; для истёкших договоров добавлены действия «Архивировать» / «Восстановить» с вкладкой «Архив».
- В настройках гостиницы добавлен блок «Географическая привязка» (страна, регион, республика, район, город, адрес) и переключатель «Завтрак включён в стоимость».
- В тарифах авиакомпании добавлен такой же географический блок; сопоставление тарифов и гостиниц учитывает уровни адреса.
- В карточке заявки добавлено редактируемое поле «Фактический заезд» (`actualCheckInAt`) с валидацией на бэкенде.
- В разделе «Сотрудники» добавлен экспорт списка сотрудников авиакомпании в XLSX.

### v12.7 (22.05.2026)
- «Готовность к работе»: добавлен бейдж `ReadinessIndicator` с дробью «выполнено/всего» и поповер с чек-листом для гостиниц, авиакомпаний и отделов диспетчера; показывает недостающие шаги настройки по вкладкам (`hotelReadiness.js`, `airlineReadiness.js`, `dispatcherDepartmentReadiness.js`).
- На странице авиакомпании (для администраторов авиакомпании) рядом с названием отображается бейдж готовности; в списке гостиниц бейдж готовности скрыт от роли авиакомпании.
- Браузерные push-уведомления о новых заявках, продлениях, резервах и сообщениях чата — подключены к существующим GraphQL-подпискам (хук `useBrowserNotifications`).
- Новые отделы при создании сразу получают все флаги доступа и уведомлений; в панель доступа добавлен переключатель «Включить всё / Выключить всё».
- В сайдбары пользователей (диспетчера и авиакомпании) добавлен инлайн-создатель должности «+ Добавить должность»; в создание пользователя авиакомпании добавлено поле «Телефон».
- Раздел «Реестр договоров» скрыт для отделов авиакомпании.
- Рядом с аватарами пользователей отображается зелёная/красная точка онлайн/офлайн (видна супер-админу); в `graphQL_requests.js` добавлено поле `online`.
- В реестре договоров добавлена колонка «Дата окончания»; сортировка по приоритету истечения: сначала истёкшие, затем истекающие в течение 90 дней.
- После создания/редактирования отдела авиакомпании ответ возвращает полную матрицу доступа, уведомлений и должностей — UI обновляется без повторного запроса (`CREATE_AIRLINE_DEPARTMERT`).
- В тарифах гостиницы для питания и доп. услуг столбец «Цены» разбит на три: «Цена без НДС», «НДС (5%)», «Цена с НДС».
- Маска ввода телефона на формах создания и редактирования пользователей.
- ФАП v2: внешние пользователи гостиниц (вход по CRM-ссылке) попадают на `/fapv2/{id}/service/living`, видят только свою гостиницу, получают сайдбар чата и кнопку выхода на страницах детали, услуги и отчёта.
- Кнопки «PWA» переименованы в «Сканер», «CRM» — в «Сайт».
- Бейджи непрочитанных сообщений показывают число (1, 2, …, 99+) вместо точки.
- TravelLine: новая вкладка «Корп. клиенты» — создание по ИНН/КПП, список, поиск (`CorporatesTab`); селектор корпоративного тарифа в модалке брони; КЗПВ (ранний заезд / поздний выезд) в `PropertyModal` и `ReservationDetailModal`; модалка «Изменить бронь» с пересчётом доп. услуг.
- Фильтры списков гостиниц (город, звёзды, оценка, поиск, страница) сохраняются в search params URL — кнопка «Назад» из карточки гостиницы/авиакомпании возвращает в список с теми же фильтрами; пагинация внутри реестров договоров больше не требует двойного нажатия «Назад».

### v12.8 (04.06.2026)
- ФАП v2: добавлено разделение состава заявки на «Экипаж» и «Пассажиры» — чекбоксы, мультиселект состава (`FapCrewModal`, `PersonTypeToggle`).
- Трансфер разделён на двусторонний: отдельные карточки и страницы для `ARRIVAL` и `DEPARTURE`.
- Единый плавающий чат: компонент `FapChat` подключён ко всем подразделам; для устранения мерцания меню при переходах добавлен общий маршрут `FapLayout`. Чат стал плавающим перетаскиваемым окном с тремя состояниями (мини / большое / свёрнутая вкладка), состояние сохраняется в `localStorage`.
- Заголовок страницы детали заявки ФАП v2 переработан: добавлен логотип авиакомпании и переиспользуемая кнопка `FapActionButton`; чат вынесен в слайд-дровер справа с красным бейджем непрочитанных на FAB; возможность редактирования экипажа внутри панели редактирования услуги; иконки close/cancel/edit принимают проп `color`.
- Навигация на странице детали упрощена до единой сетки KPI-плиток (режимы «Карточки» и «Сплит» убраны); у плиток — цветные рамки только при наведении (через CSS-переменные).
- Новая полноэкранная страница услуг «Поставка воды» и «Поставка питания» (`FapWaterMealPage`): KPI-плитки, строка быстрого добавления с маской телефона, инлайн-редактирование и удаление, скрытие при выполненном плане.
- Карточки заявок ФАП v2 переоформлены: логотип авиакомпании, многострочные ряды иконок, точки услуг (`ServiceProgressDot`).
- Поле `requestNumber` теперь отображается везде; подключена маска телефона через `@react-input/mask`.
- Тарифы гостиницы переработаны под НДС-режим: цены показываются двумя строками («С НДС» и «Без НДС») с единой колонкой-чипом «НДС»; секции «Питание» и «Доп. услуги» получают кнопку «Редактировать» с общими сайдбарами (`InfoTableDataTarifs`, `EditRequestMealTarif`).
- Для выбора города гостиницы добавлен `CityRegionPicker` — выбор из справочника регионов вместо свободного ввода республики/района.
- Трансфер в ФАП пересобран на список водителей (`FapTransferPage`) + страница конкретного водителя (`FapDriverPage`) по аналогии с проживанием: вкладки «Состав» / «Маршрут», ссылка «Открыть на карте» Yandex Maps, инлайн-CRUD пассажиров и экипажа; KPI-полоса унифицирована к «N / N чел.» по всем услугам.
- Добавлена страница «Багаж» (`FapBaggagePage`) с потоком водителей и доставок; KPI-плитка «Багаж» показывает количество доставок (выполнено/всего) вместо «0 чел.».
- Сайдбар тарифа трансфера в гостинице (`EditHotelTransferTarif`): отдельные цены «Аэропорт → Гостиница» и «Гостиница → Аэропорт» для договорных и авиакомпанийских ставок.
- Для тарифов питания, трансфера и доп. услуг добавлен режим «По запросу» — когда условия пока не согласованы.
- В списках гостиниц и авиакомпаний — серверный поиск с debounce и фильтр по аэропорту.
- Роль авиакомпании в ФАП переведена в режим только чтения: запрет на добавление, редактирование и удаление услуг, гостиниц, водителей; список заявок ФАП принудительно фильтруется по своей авиакомпании, селектор авиакомпании скрыт.
- Маршруты и идентификаторы пунктов меню переименованы: `/fapv2` → `/far` (FAP → FAR).
- Бейдж готовности гостиницы расширен: учитываются цены трансфера и доп. услуги (проверки только для диспетчера); показ зависит от роли.
- В списке гостиниц 4 встроенных фильтра свёрнуты в кнопку-поповер «Фильтры» с бейджем количества активных фильтров и сбросом; строка поиска растягивается, чтобы оставаться видимой на экранах 1366px.
- В формах создания заявки и редактирования услуги ФАП добавлен инлайн-шорткат «Создать сотрудника» — открывает сайдбар `CreateRequestAirlineStaff` и автоматически добавляет нового члена экипажа в выбор.
- В отчёте по гостинице ФАП (`FapHotelPage`) добавлено автосохранение с дебаунсом и финальной отправкой при размонтировании; сопоставление строки тарифа в отчёте идёт только по ФИО — несовпадение номера комнаты не сбрасывает тариф.
- В каждой услуге добавлено отдельное поле «Плановая дата».
- Возможность удалять дополнительные услуги из вкладки тарифов гостиницы.

### v12.9 (28.06.2026)
- ФАП: в отчёте по гостинице гости маркируются как «взрослый / ребёнок / младенец» (бейдж в списке гостей и отчёте, отдельная колонка в XLSX); в подборе тарифа отчёта показываются собственные тарифы гостиницы по виду номера (только для чтения); отчёт сгруппирован по номерам, проживание считается один раз на номер (исправлено двойное начисление).
- ФАП: добавлен каталог-подбор для массового добавления пассажиров из справочника заявки (`CatalogPickerModal`) — поиск, мультивыбор, отметка уже добавленных; подключён в услугах воды/питания, проживания и трансфера; добавлены массовые операции добавления людей.
- ФАП v2: проведена внутренняя чистка кода без изменения поведения (удаление мёртвого кода, переиспользование общих хелперов и хуков).
- «Эскадрилья»: импорт XLSX объединён с «Создать заявку» в переключатель «Одиночная / Массовая» (только для супер-администратора) с клиентским разбором файла; добавлена группировка заявок («Список / Группы») с дашбордом-плитками (логотип, количество, полоса готовности) и детальной страницей группы; статус-плашки и сегмент-переключатель переоформлены, убраны разделители строк.
- Карточка «О гостинице» переоформлена под референс-дизайн: единая карточка с вкладками «Общая информация / Номера / Тарифы» — шапка с аватаром, названием и классом, галерея с лайтбоксом, правая колонка карточек «Информация / Контакты / Питание», сетка номеров с модалкой и тарифы с колонками «без НДС / НДС 5% / с НДС». Работает везде, где используется компонент, включая публичный предпросмотр.
- Дополнительно во вкладке «О гостинице»: звёздность и рейтинг выводятся отдельными строками, «Вместимость» переименована в «Мощность», добавлена карточка «Адрес», контакты гостиницы заменены на единые контакты Kars Avia (e-mail и телефон с иконками), рядом с названием — бейдж «Выгода от N %».
- Введён единый фильтр-поповер (`FilterPopoverButton`): квадратная кнопка-иконка с бейджем количества активных фильтров и кнопкой «Сбросить»; на него переведены списки гостиниц, «Эскадрилья», «Трансфер» и все реестры договоров (авиакомпаний, гостиниц, организаций).
- Добавлен компонент звёздного выбора оценки (`StarRatingFilter`): интерактивные звёзды 1–5 (целые или с половинами) с числовым полем; применён в фильтре гостиниц (Оценка / Звёздность) и в форме создания гостиницы.
- Разделы «Об авиакомпании» и «Об организации» переоформлены в единый карточный стиль настроек гостиницы: левая навигация по секциям с иконками (Общая информация, Реквизиты, Контакты, Адрес, График). Вкладка настроек гостиницы приведена к той же карточной системе.
- «Трансфер»: фильтрация переведена на сервер — добавлены фильтры по авиакомпании, организации и водителю и мультивыбор статусов (включая новый статус «В пути к месту назначения»); исправлена пагинация (смещение строк вместо номера страницы); фильтры собраны в поповер.
- Реализовано системное уведомление о релизе «Что нового»: при входе авторизованный пользователь один раз видит модальное окно с версией, заголовком и списком изменений (кнопка «Понятно» помечает версию просмотренной); публикация и редактирование релиза — только для супер-администратора, с живым предпросмотром.
- Создана единая страница «Системные уведомления» с pill-переключателем между разделами «Что нового» и «Плашка техработ»; настройки плашки техработ переоформлены в режим «форма + живое превью», индикаторы-точки показывают включённость каждого раздела.
- География тарифов авиакомпании переведена на список (`TariffGeographyList`): один тариф можно привязать к нескольким городам/регионам; при сохранении требуется указать хотя бы один город/регион или аэропорт; в карточке тарифа добавлен блок «География».
- В меню диспетчера добавлены счётчики новых заявок по разделам «ФАП» и «Трансфер» (через подписки).
- Ленивая подгрузка списков (бесконечный скролл) вынесена в переиспользуемый хук `useInfiniteScroll` и компонент-якорь `InfiniteScrollSentinel`. На них переведены уведомления (`NotificationsSidebar`) и истории изменений гостиниц/авиакомпаний/резерва и заявок (`LogsHistory/Logs`, `LogsHistory/PassengerRequestLogs`) — нумерованная пагинация заменена догрузкой по скроллу; устаревший дубль панели уведомлений `Notifications` удалён. Попутно устранены гонка двойных запросов при быстром скролле и неверный расчёт наличия следующих страниц.

### v12.10 (29.06.2026)
- Добавлена страница «Должности и доступ» (`PositionAccessPage`) для авиакомпаний и диспетчеров: создание, переименование и удаление должностей, а также настройка матрицы прав отдельно для каждой должности — наследование от отдела или собственный набор флагов. Открывается из настроек по маршруту `positions`; добавлены кнопка `PositionsAccessButton` и выпадающее меню `AddMenuButton` («Новый отдел / Новый пользователь»).
- Эффективный доступ пользователя (`effectiveAccessMenu` — с учётом переопределений по должности) подключён в `Main_Page` через запрос `GET_USER_EFFECTIVE_ACCESS_MENU`: меню отдела служит базовым слоем, эффективный доступ накладывается сверху (`safeAccessMenu`).
- Конвертер прав вынесен в общий модуль `src/utils/accessPayload.js` (`buildAccessPayload`, `ALL_TRUE_ACCESS`) и переиспользован в `AccessSettings`, `DispatcherAccessSettings`, `SettingsSidebar` и `AccessPermissionsPanel`.
- В GraphQL добавлены мутации `UPDATE_POSITION` / `DELETE_POSITION` и поле `accessMenu` в запросах должностей.
- Бейджи-счётчики в меню диспетчера переведены на «тонкие» запросы `*_COUNT` (`GET_REQUESTS_COUNT`, `GET_PASSENGER_REQUESTS_COUNT`, `GET_TRANSFERS_COUNT`): вместо загрузки полных списков `take: 999999999` берётся только `totalCount`.
- Меню супер-администратора переоформлено в виде «рельса иконок»; структура пунктов меню приведена к единому виду.

### v12.11 (29.06.2026)
- Добавлен единый компонент загрузки аватара/изображения с обрезкой `AvatarUpload` (`src/Components/Blocks/AvatarUpload/`) на базе `react-easy-crop`: drag-and-drop или выбор файла, модалка обрезки (круглый кроп, масштабирование), превью текущего файла с действиями «Заменить» / «Удалить», проверка типа и размера (до 8 МБ), сообщения об ошибках через `onError`.
- Сырые `<input type="file">` заменены на `AvatarUpload` в формах создания/редактирования заявок и профиля (`CreateRequestAirlineCompany`, `CreateRequestCompanyHotel`, `CreateRequestDispatcherCompany`, `EditRequestAirlineCompany`, `ExistRequestCompany`, `ExistRequestCompanyHotel`, `ExistRequestProfile`) и во вкладках «Об авиакомпании», «Об организации», «Настройки гостиницы».
- Добавлены иконки `CheckCircleIcon` и `ImageIcon` в `src/shared/icons/`; вариант `variant="image"` показывает иконку изображения вместо заглушки аватара.
- Из `HotelSettings_tabComponent` удалён крупный устаревший закомментированный блок legacy-кода.

### v12.12 (30.06.2026)
- Боковые меню не-суперадминов (диспетчер, авиакомпания, гостиница) сгруппированы по разделам с заголовками и сделаны компактнее: пункты разбиты на группы («Заявки», «Организации» / «Размещение», «Управление» / «Моя компания» / «Компания», «Аналитика» / «Документы», «Сервис»); уменьшены отступы, шрифт и размер иконок. В свёрнутом меню заголовки групп заменяются тонкими разделителями, а пустые по доступу группы скрываются. Все SVG навигации вынесены в общий модуль `src/shared/icons/menuNavIcons.jsx`, а сами меню (`DisAdminMenu`, `AirlineAdminMenu`, `HotelAdminMenu`) переписаны на data-driven рендер с сохранением маршрутов, проверок доступа, бейджей-счётчиков и подсказок. Меню супер-администратора («рельс иконок + панель») не затронуто.
- ФАП: возрастная категория пассажира (взрослый / ребёнок / инфант) распространена на все услуги и отчёты. Селектор категории при добавлении/редактировании и бейдж в списке добавлены в «Проживание» (`FapHotelPage`), «Трансфер» (`FapDriverPage`) и «Поставку воды/питания» (`FapWaterMealPage`); колонка категории добавлена в отчёт по гостинице (`FapReport`). Бейдж вынесен в общий компонент `CategoryBadge`, а константы и хелперы (`PERSON_CATEGORY_LABEL`, `PERSON_CATEGORY_BADGE`, `PERSON_CATEGORY_OPTIONS`, `normalizeCategory`) — в `fapConstants`; категория «взрослый» теперь также отображается явным бейджем. В запрос `GET_PASSENGER_REQUEST` добавлено поле `personCategory` (водитель и вода/питание). В выгрузку XLSX (лист по гостинице и сводный лист) добавлена колонка «Возрастная категория» с соответствующим сдвигом столбцов, формул «Итого» и объединений ячеек.
- «Эскадрилья»: переключатель вида «Список / Группы» доступен только супер-администратору; для остальных ролей всегда отображается список (как сохранённое значение, так и сам тумблер скрыты).
- Тарифы авиакомпании: требование и поле географии (город/регион, `TariffGeographyList`) временно отключены в формах создания и редактирования тарифной категории — до доработки массива географии на бэкенде.

### v12.13 (27.07.2026)
- ФАП, отчёт по проживанию: тариф стал посуточным (цена за каждую ночь отдельно), добавлены вид размещения и дата рейса; экран получил режимы «Просмотр» / «Редактирование» с переключателем и панелью действий в доп. меню; правки, сделанные другим пользователем, подхватываются на живом экране; строки отчёта сопоставляются с гостями по `personId`, а не по ФИО, поэтому выселение из середины списка больше не сдвигает данные на соседа.
- ФАП: тариф гостиницы получил режим начисления «Койко-место» / «Номер» — при «Номере» проживание начисляется один раз на номер (управляется стороной перевозчика), питание считается на каждого гостя; проживание вынесено на шапку номера, а не на первого гостя.
- ФАП: в тариф добавлен ланчбокс (тумблеры у завтрака / обеда / ужина) и количества приёмов пищи по каждому гостю с панелью «Кол-во всем» и точечной правкой; выгрузка отчёта расширена до 24 колонок. Цены питания в строке гостя берутся только из тарифа; формула расчёта проживания перенесена в подсказку «?» в стиле системных поповеров.
- ФАП: заведён отдельный реестр пассажиров страницей (`FapRegistryPage`), терминология «гость» приведена к «пассажиру»; добавлены группы пассажиров (семья / коллеги) — чипы с типом связи, предупреждения при расселении, секции в пикере, групповые действия и требование вида размещения; метки групп продублированы во вкладке «Отчёт», сортировка по умолчанию ставит связанных пассажиров наверх.
- ФАП: импорт манифестов переведён на профили форматов — поддержаны ПМ и ПНЛ (`manifestProfiles`), перед импортом рейс манифеста сверяется с рейсом заявки (плашка + подтверждение); загрузка файлов манифеста и массового импорта «Эскадрильи» переведена на общий `FileDropzone`.
- ФАП: добавлена вкладка аналитики «Пассажиры» — модалка фильтров, KPI, таблица заявок с раскрывающейся детализацией, сводки по измерениям, графики и единый поток выгрузки в XLSX одной книгой; в аналитику добавлены связи пассажиров (колонка «Группы», KPI «Связано пассажиров»). Списки и модалка фильтров переоформлены визуально, таблица и графики получили общий скролл.
- ФАП: добавлена доставка багажа — поездка водителя со списком пассажиров, у каждого свои номера бирок, цена и адрес доставки; заведена отдельная страница поездки, сумма считается по пассажирам; состав берётся только из реестра с мультивыбором; шапка услуг унифицирована (реестр, отчёт и редактирование доступны из любой услуги).
- ФАП: список заявок (`/far`) переведён на бесконечный скролл вместо `take: 100`, `useInfiniteScroll` расширен методом `refreshWindow()`; добавлен фильтр по периоду.
- Цены авиакомпании: договоры разделены на индивидуальные и общие (`contract-type split`) с проверкой конфликтов географии; формы цен стали презентационными — сохранение, рефетч и уведомления вынесены в родителя (`utils/airlineTariffPrices.js`), список обновляется оптимистично с откатом при ошибке. Цены трансфера: сохраняется только изменённая запись, добавлены вместимости 20 и 50 мест.
- Реестр договоров: подтверждение при закрытии формы показывается только если данные введены; карандаш открывает сайдбар сразу в режиме редактирования (то же — у карточки «ГК Карс»).
- Во всех формах создания обязательные поля отмечены красной звёздочкой.
- TravelLine (сертификация): дедлайн отмены с часовым поясом в диалоге отмены, постоянная панель часового пояса заезда / выезда, часовой пояс в списке броней, вкладка корпоративных клиентов, `cancellationPoliciesJson` из сайдбара заявки; расшифровка штрафа (ставка + дополнительные сутки) в предупреждении РЗПВ и в блоке альтернативы; цена альтернативы считается за всё проживание и больше не умножается на количество ночей. Гостиницам добавлен статус.
- Эффективный доступ по должности (`useEffectiveAccessMenu`) подключён в роут-компонентах; отчёты полностью открыты авиакомпаниям, заголовки колонок отчёта по трансферу переименованы.
- `AddressField` больше не портит сохранённый адрес при быстром вводе и повторном открытии формы.
- Приложение представителя: распознавание документа пассажира по фотографии (Yandex Vision → GPT) с извлечением телефона.

### v12.14 (14.08.2026)
- Добавлен раздел **«Отчёты v2»** (`src/Components/Blocks/ReportsV2/`, маршрут `/reportsV2`, роли `SUPERADMIN` и `DISPATCHERADMIN`) — отчёты по заявкам эскадрильи. Поверх обычного создания отчёта: черновики (`createAirlineReportDraft` / `createHotelReportDraft` → правка строк → `confirmReportDraft`, включаются галкой «Проверить строки перед выгрузкой») и пороги частичных суток (`ReportPartialDaySetting`, уровень `GLOBAL`; правит `SUPERADMIN`, `DISPATCHERADMIN` — тоже, вопреки документации бэка). Раскладка: `ReportsV2.jsx`, `ReportsV2List/`, `ReportDraftsPanel/`, `ReportDraftEditor/`, `ReportCreateSidebar/`, `ReportRulesSidebar/`; доменная логика без JSX — `reportRules.js`, `reportDraftRows.js`, `reportDraftAge.js`, `releasedReports.js`, у всех тесты рядом (`node --test src/Components/Blocks/ReportsV2/`).
- «Отчёты v2», редактор черновика: таблица повторяет печатную форму реестра дословно (те же 18 колонок, заголовки и порядок) — экран сверяют с выгруженным файлом построчно; колонки «№» и «ФИО» закреплены с границей и полосой состояния строки; добавлены предпросмотр «как напечатается», список сожителей по номеру и сверка итогов; строки группируются по гостиницам (переключатель «Файл / Гостиницы», по умолчанию «Файл») со сводкой «строк · предупреждений · суток · сумма», при активном фильтре суммы из шапки группы убираются. В шапке — логотип авиакомпании и подсказки с разбивкой сумм по гостиницам в стиле подсказок ФАПа.
- «Отчёты v2»: выпущенный отчёт открывается на экране в режиме только чтения — тем же `ReportDraftEditor` с пропом `mode="view"`, вход рисуется только у отчётов, для которых по `ReportDraft.savedReportId` нашёлся черновик; добавлена кнопка «Скачать файл» и иконка `ViewReportIcon` (документ с лупой). Мелкие правки: ноль в полях ввода показывается подсказкой, а не значением; сутки печатаются «4,5» и «12» без хвостов; склонение «суток» вынесено в общий `src/utils/plural.js`.
- Права доступа: заведены ключи `accessManage` (строка «Управление доступами» в карточке «Пользователи»; гейтит шестерёнку отдела и кнопку «Должности и доступ», видна только тому, у кого право уже есть), `travellineMenu` (секция «TravelLine») и `reserveUpdateCompleted` («Редактирование завершённой заявки» в секции ФАП, по умолчанию выключено). Описание секций вынесено в единый `SettingsSidebar/accessSections.js` с тестом на соответствие ключей; маршруты `positions` и `travelline` закрыты соответствующими правами; панель прав сбрасывается при отмене редактирования.
- Настройки: дублирующие `AccessPermissionsPanel` слиты в один компонент, три панели уведомлений слиты в одну, неработавшие переключатели каналов уведомлений подключены (`utils/notificationPayload.js`), удалены 4 недостижимые страницы настроек и пропсы, жившие только ради них; управление доступом переехало в карточку «Пользователи».
- TravelLine: раздел вынесен отдельным пунктом меню диспетчера, страница получила собственную шапку; сайдбар номеров спрашивает корпоративного клиента, привязанного к юрлицу, и передаёт его и в поиск доступности, и в бронирование — закрытые корпоративные тарифы дошли до потока, в котором работает диспетчер (единственный клиент подставляется молча, смена клиента сбрасывает выбранный тариф). Заглушка, скрывавшая гостиницы TravelLine при выборе, снята.
- Цены авиакомпании: добавлено поле «Применяется к» (`contractType` — Эскадрилья / ФАП / Все), у аэропортов появился адрес; цены заявок эскадрильи подбираются по типу договора, конфликтующие локации отсекаются при переключении типа (`utils/airlineTariffPrices.js` + тесты).
- ФАП, расчёт суток: проживание считается по длительности — минимум сутки, дальше +0,5 за каждый начатый 12-часовой блок (`calculateCostDaysByDuration` в `utils/effectiveCostDays.js` + тесты); `daysCount` пересчитывается при открытии отчёта; счёт ночей унифицирован на обоих экранах, вручную введённое значение переживает перезагрузку.
- ФАП, отчёт по проживанию: добавлены редактируемый вид размещения номера (единый источник) и колонка «Скидка» с дефолтами 0 / 50 / 100 % по возрастной категории; цены авиакомпании стали третьим, договорным источником тарифа (`fapAirlineTariff.js`, подбор по `contractType` + аэропорту, цена по категории номера, редактируемый оверлей); ланчбокс получил независимое количество (`lunchboxCount`) и редактируемую цену в тарифах гостиницы, при нулевой цене показывается предупреждение; несохранённые черновики тарифов переживают пересборку отчёта; перед отправкой авиакомпании показывается предполётная сводка недосчитанного.
- ФАП, видимость отчёта: отчёт по гостинице открывается авиакомпании только после кнопки «Отправить на проверку» (`submittedAt`, гейт на 6 точках включая аналитику, серверная фильтрация и бэкфилл истории); изменение состава или замена гостиницы снимают отметку. Бейдж «отправлен на проверку» переехал в шапку гостиницы.
- ФАП, изоляция данных: гостиничные роли внутри общей заявки видят только свою гостиницу (`isHotelScoped` / `scopedHotelId` в `utils/access.js`) — раньше администратор одной гостиницы открывал карточку и отчёт чужой; ссылки-входы участников (`linkCRM` / `linkPWA` гостиниц и водителей) показываются только диспетчеру (`canSeeExternalLinks`), возврат услуги в работу — тоже (`canReopenPassengerService`). На бэке заявки скоупятся по субъекту (сначала в режиме наблюдения), все резолверы аутентифицированы, сканер документов ограничен по частоте.
- ФАП: добавлена мутация `reopenPassengerRequestService` и пункт «Вернуть в работу» на четырёх экранах услуг; завершённая заявка открывается только для просмотра, исключение выдаётся правом `reserveUpdateCompleted`; экраны услуг отменённой заявки заблокированы; причина обязательна при отмене заявки.
- ФАП, манифесты: поддержан формат ПЛИ, младенцы на руках импортируются отдельными строками реестра, чип помечен как «инф», текст предупреждения говорит только то, что видно в манифесте; форматирование дат Excel вынесено в тестируемый `utils/excelDate.js`.
- ФАП, размещение: заселение сверх заявленного числа мест разрешено во всех трёх репозиториях, диспетчеру приходят предупреждения о расхождении плана и факта (перебор, совпадение ФИО); номер места различает однофамильцев; выселение, переселение, удаление и назначение номера переведены на массовые мутации; в уведомлениях и истории гостиницы называются по имени, а не по индексу; адрес гостиницы при подстановке в заявку склеивается с городом (`utils/hotelAddress.js` + тесты и миграция на бэке).
- ФАП, трансфер и багаж: поездка привязывается к гостинице (`hotelItemId`), появилось поле «перевезено» (`transportedCount`, факт = max(список, число)) с выводом в KPI, плитку, карточку и колонкой «Перевезено» в выгрузке; адрес аэропорта подставляется в маршрут водителя; доставку багажа можно отметить и после выполнения плана. Подсказки групп распознают женскую форму фамилии (`utils/surnameForms.js` + тесты).
- ФАП, прочее: номер рейса правится в сайдбаре заявки, ссылка на представителя оформлена системным чипом, все выпадающие списки переведены на `FapSelect` (починено закрытие меню при внутренней прокрутке), устаревший экран отчёта первой версии (`RepresentativeHotelReportPage`) удалён; закрыта пачка дефектов реестра и аудита модуля от 11.08.
- Шахматка гостиницы оптимизирована: запросы переведены с `network-only` на `cache-and-network`, контекст авторизации мемоизирован, лишние перезапросы убраны, подписки обновляют список точечно; на бэке переписан ряд резолверов размещения.
- Лимит размера тела запроса поднят до 2 МБ (`services/infra/httpLimits.js`, зеркало на фронте — `SAVE_PAYLOAD_LIMIT_BYTES` с падающим при расхождении тестом): большие черновики отчётов сохраняются целиком.
- Меню: счётчик на первом пункте свёрнутого меню больше не обрезается, иконки выровнены по центру фиксированной обёртки. Внутренние документы (`docs/internal/`) выведены из-под контроля версий.
- Справочник категорий номера вынесен в единый модуль `src/utils/roomCategories.js` (+9 тестов): до этого один и тот же словарь лежал шестью копиями (номерной фонд, `ExistRequest`, `fapRooms`, две формы тарифа гостиницы, `fapAirlineTariff`) и успел разъехаться, а список полей цен дублировался ещё в четырёх формах авиакомпании. Вместимость и поля цен в модуле — копия серверных карт `categoryToPlaces` и `getCategoryPriceFromContract`, тест падает при расхождении с энумом `Category`. Подключены четыре категории, добавленные бэком 10.08: «Эконом одноместный», «Стандарт одноместный», «Стандарт дабл», «Делюкс» — в формах тарифа гостиницы, номерном фонде, ценах авиакомпании и подборе тарифа ФАП; четыре выборки цен в `graphQL_requests.js` приведены к одному виду (в двух из них не запрашивались ещё и `priceComfort` / `priceImprovedComfort`).
- Номерной фонд гостиницы: массовое удаление номеров через мутацию `deleteManyRooms`. Кнопка «Выбрать» включает режим отметок — чекбоксы у номеров и у шапки категории (шапка выделяет только видимые под текущим фильтром и поиском), удаление идёт одним запросом с подтверждением через общий `useDialog`. Мутация всё-или-ничего: при неизвестном id бэкенд не удаляет ничего.

### v12.15 (02.09.2026)
- «Отчёты v2» стали разделом **«Отчёты»** для всех ролей, кроме супер-администратора (`ebbe704`): у диспетчера `/reports` открывает v2, `/reportsV2` — алиас; у авиакомпании гейты `reportMenu` / `reportCreate`, у гостиницы без гейтов. Черновики и «Правила расчёта суток» — только `SUPERADMIN` и `DISPATCHERADMIN` (`reviewEnabled = canDraft && withReview` во всех развилках выпуска и в подписи кнопки); тип отчёта форсится по привязке учётки (`me.hotelId` / `me.airlineId`), гостинице в создании убран пикер авиакомпании; заголовок и текст пустого списка — по аккаунту.
- Отчёты: **архив** — переключатель «Текущие · Черновики · Архив» в тулбаре (общий `SegmentedToggle` получил `variant="toolbar"` и `count`), `filter: { archived: true }`, мутации `archiveReport` / `restoreReport`, удаление только в архиве по `canDeleteReport` (`reportsV2Access.js` + тест) — супер, диспетчер-админ или новый ключ **`reportDelete`** (`roles.js`, `accessPayload.js` 34 → 35, `accessSections.js` как `extras`, чтобы не схлопывался в общий тумблер). Бэк с `e724a77` отдаёт без флага только две последние декады, старые отчёты архивирует крон.
- Отчёты: **черновик авиакомпании через `SUBMITTED`** — `submitAirlineReportDraft` / `unsubmitAirlineReportDraft`, один `GET_REPORT_DRAFTS` без `status` с делением на клиенте (`splitDraftsByStatus` + тест); у диспетчера панели «Незавершённые черновики» / «У авиакомпании на подтверждении», у АК — вкладка «На подтверждении» с «Подтвердить и выгрузить»; `canEdit = mode === "edit" && status === "DRAFT"`; карточка уведомления по `description.action === "submit_airline_report_draft"`. Черновики вынесены отдельной вкладкой вместо панелей над списком, поиск на ней скрыт (`c5f8ee5`, `1000c4f`).
- Отчёты: панель «Правила расчёта суток» — уровни `GLOBAL / AIRLINE / HOTEL` (`0b9711c`): `pickSetting` по level+id (бэк сортирует `level asc` по алфавиту enum), upsert всегда без `id`, «Сбросить к общим» через `DELETE_REPORT_PARTIAL_DAY_SETTING`, бейджи «переопределено». Подсветка суток в черновике берёт действующие пороги (`resolveDraftPartialDayRules` — зеркало бэкового `resolvePartialDayRules`, правится парой), завтрак в цене печатается «вкл» (`breakfastCellText`) (`b554299`). Даты периода рендерятся в UTC — конец периода больше не +1 день (`56ee576`).
- ФАП, **согласование цен отчёта по проживанию** (`fapReportAccess.js` + тесты): `hotelReportPricingApprovedAt` / `isHotelReportPricingApproved` / `airlineMoneyHidden`; сегмент «Цены не согласованы / Цены согласованы» в тулбаре `FapHotelPage` при `canEdit && reportSubmitted && !hideMoney` (гостинице тумблера нет), мутация `setPassengerRequestHotelReportPricingApproved`; у АК между submit и approve — баннер и `FapReportView` с `hideMoney`; все четыре точки выгрузки (`FapDetail`, `FapHeaderActions`, `FapLivingPage`, `handleExport` самой страницы) без денег, пока хоть один видимый АК отчёт не согласован. Бэк маскирует денежные поля строк для АК до `pricingApprovedAt`; бэкфилл `services/migrations/approvePricingForSubmittedReports.js` (`--dry-run` / боевой, ловушка `OR: [{ null }, { isSet: false }]`).
- ФАП, **манифест во вложениях** (`9147452`, `248c7b7`, `b18cd0f`): исходный `File` после `addSavedPeople` уходит в `ADD_PASSENGER_REQUEST_FILES` в трёх точках импорта (`CreateRepresentativeRequest`, `AddRepresentativeService`, `FapRegistry`), имя нормализуется в `manifest-<рейс>.<ext>` (бэковый `safeSlug` режет кириллицу); `GET_PASSENGER_REQUEST` выбирает `files`; чип `FapManifestFiles` в шапке `FapDetail` и в `FapHeaderActions` всех страниц услуг под гейтом `!isExternalUser && !isHotelScoped`, несколько файлов — через `FapOverflowMenu` с новым пропом `trigger`; хелперы в `fapManifestFiles.js` (+12 тестов). Иконки чипов «Манифест» / «Реестр» — штриховые `FileDownIcon` / `UsersIcon`.
- ФАП, **деление цены номера** (тариф «Номер», `fapRoomSplit.js` + 9 тестов): `T = P × D_несущего` делится между жильцами по весам `1 − скидка/100` (категория 0 / 50 / 100 % или ручная), копейки остатком несущему, `Σ = T`; `Σ = 0` → фолбэк «всё на несущем»; отправленные отчёты читаются как документ (`savedPricePerDay`, `savedPlacementKind`). Догон базы: `reportMoneyDiffers(built, saved)` в `fapReportMoney.js` (+7 тестов) и эффект в `FapHotelPage` — черновик при открытии сохраняется один раз на снимок, если живой расчёт разошёлся с базой (книга из шапки заявки печатает базу) (`5642724`).
- ФАП, форматы манифеста: текстовая ведомость фиксированной ширины (`manifestFixedWidth.js`, `f83bac6`), ICAO-манифест с двумя записями в строке (`049b63a`), профиль Руслайн (`manifestProfiles.js`, `abd0cb4`) — шесть профилей всего.
- ФАП, xlsx (`d48064a`, `dfe20d6`, `13e7a8a`): `nameFull` в шапках, Times New Roman / границы / денежный формат / freeze через `finishSheet`, лист «Доставка багажа» (итог — перечислением водительских ячеек, не SUM), вид размещения соседей по номеру и группировка гостей по номеру, блок «Трансфер» листа гостиницы только по включённым направлениям, лишний город убран из заголовков; первые тесты в `reports/` (`node --test src/Components/Blocks/FapV2/reports/`).
- ФАП, видимость для гостиницы: `fapServiceVisibility.js` (`HOTEL_RESTRICTED_SERVICE_KEYS = transfer, transferDeparture, baggage`) + хук `useHotelServiceVisibility` вместо трёх копий `GET_HOTEL_TRANSFER_PRICE`; плитки, редирект-гарды `FapServicePage` / `FapDriverDetailPage` / `FapBaggageTripDetailPage`, `hiddenServiceKeys` в книге (`8e5042a`); гостиница с одним проживанием попадает с деталки сразу на свою страницу, «Назад» ведёт в `/far`, ряд иконок услуг в списке скрыт (`a1ecc37`). На бэке гостиничные субъекты изолируются безусловно (`isHotelSubjectScope`), гейты `assertHotelScopeAccess` в 14 точках, `HOTELUSER` в списках гостиничных ролей, `passengerAnalytics` — 403 гостиницам (`e5ee1f9`, `978803c`).
- ФАП, **деньги отчёта скрыты от гостиницы** (`b824fc2`): `hideMoney = isHotelScoped(user)` — вкладка «Тарифы», пикер тарифа, ставки, итоги, деньги в `FapReportView` и колонки её Excel; `preserveMoneyFields(builtRows, savedRows)` в `fapReportMoney.js` замораживает `pricePerDay` / `accommodationCost` / `daysCount` и ставки питания в её сейве (`foodCost` пересчитывается замороженными ставками по новым счётчикам), «Сут.» read-only. Остаточная гонка «диспетчер сохранил, пока у гостиницы устаревший saved» — до бэк-мержа по скоупу.
- ФАП, дефекты: завершённая услуга трансфера снова принимает пассажиров — новый предикат `canManageServicePeople` в `fapEditAccess.js` (запрет только `CANCELLED`) (`85ff7da`); имена тарифов сравниваются через `tariffNameKey` (trim + схлопывание пробелов + регистр) в семи точках восстановления `FapHotelPage` — ручной двойник с нулевой ценой больше не заводится (`c734197`); бейдж «ФАП» в меню фильтрует `passengerRequests` по `user.airlineId` (`cdf9667`); список `/far` восстанавливает позицию прокрутки при возврате (sessionStorage `fapListScrollState`, опция `initialTake` в `useInfiniteScroll`), полоса прокрутки `#C9D2E4` (`5c194e8`).
- ЛК гостиницы — своя сторона данных (`673da97`): «О гостинице» — контакты трёхветочные (гостиница свои из `information.email` / `number`, персонал оба набора, АК и превью — хардкод Kars Avia), номера и тарифы hotel-side (`displayPrice`, без НДС-колонок), `rooms { price }` в `GET_HOTEL`, `src/utils/hotelTransfer.js` (`hotelProvidesTransfer`, +5 тестов); «Тарифы» — сетка «Цена по договору» во всех секциях, секция «Трансфер» по сырому ответу цены; настройки гостиницы без «Рейтинга», «Скидки» и «Видимости», вкладка «Контакты» — два редактируемых поля. Бэк для гостиничных субъектов отдаёт `priceForAirline` / `mealPriceForAir` / `transferPriceForAir` как null и выкидывает их на записи (`hideAirlinePrices.js`, `e724a77`).
- «О гостинице», адаптив (`24b0aab`, `f93a0b1`, `1a78b0b`): герой `aspect-ratio: 16/9` со ступенями ширины по высоте экрана, лента миниатюр и «+N» при >4 фото, `@container about` с лестницей рейла 320 / 340 / 420 и `splitMain` от 1500, `.card { height: 100% }` вместо `100vh − 210`, фейд и кламп описания («Показать полностью»); чипы удобств над описанием — `src/utils/hotelDescription.js` (`parseHotelDescription`, `extractAmenities`, +26 тестов) и 12 SVG-иконок в `src/shared/icons/`; структурный рендер «лейбл — значение» откачен владельцем, текст описания рендерится как раньше.
- Тарифы гостиницы, **сезонные цены** (`a9649ab`, `be89d79`): `src/Components/Blocks/RoomKindSeasons/` + `useRoomKindSeasons.js` + `src/utils/roomKindSeasons.js` с тестами, четыре документа `ROOM_KIND_SEASON*` в `graphQL_requests.js`; просмотр — чипы, правка — инлайн `SeasonRowEditor`, черновики сезонов в `CreateRequestTarifCategory` (`RoomKindSeasonsDraft`, после `UPDATE_HOTEL_TARIF` → `findNewRoomKindId` → `CREATE_ROOM_KIND_SEASON` последовательно, частичный успех тостом); подтверждение перед сохранением и удалением — бэк пересчитывает заявки эскадрильи за период внутри мутации; `priceForAirline` под ролью гостиницы в input не кладётся; общий разборщик `src/utils/apolloErrorText.js`.
- Шахматка, **редизайн** (`3bf3a60` … `e5b2c6a`, `c7bc02c`, `fe29d08`): `NewPlacementV2` + `components/{GridHeader, BoardToolbar, RoomRowV2, PlacementBarV2, BarPopover, UnplacedTray, TrayCardV2}` + `utils/placement{StatusStyles, Period, BarLayout, Badges}.js` (68 тестов); виды Неделя / Декада / Месяц с адаптивным `dayW = (ширина − 240) / дней` через ResizeObserver и без h-скролла, жёлтое «сегодня», старая палитра статусов edge/tint, лоток «Неразмещённые» закреплённой колонкой в потоке, портал-поповер (комната, должность, «Квота / Резерв» из `request.reserve`, питание из `mealPlan` в `GET_BRONS_HOTEL`) и на карточках лотка, клик по плашке открывает заявку (`PointerSensor` с `distance: 5`), дроп ровно на наведённую койку (`getAvailablePosition` удалён), `onDragCancel` → `resetDragState`, «Отмена» в модалке не плодит дубликаты; высота доски меряется от фактического top, лоток скрыт у гостиниц (`showTray = !user.hotelId`). Ловушки: глобальный `scroll-behavior: smooth` гасится `auto` на `.gridScroll`; `threshold.x: 0` отключает ось автоскролла. `TimelineV2` / `DraggableRequestV2` удалены.
- Шахматка, **единая доска без «Квота | Резерв»** (`42b75e5`): переключатель, резервный стейт, три модалки, `reserveId`-ветки мутаций, `AddPassengersModalV2` и три запроса / две подписки резерва удалены (−1260 строк); легаси-плашки резерва из БД рендерятся инертно. Замер: бэк с мая молча дропает `hotelChesses` с `reserveId`, деление никогда не фильтровало комнаты.
- Сносы (`164c00a`, `0389754`, `47be19d`): `PlacementDND` (v1, −3835 строк) и `react-window` из зависимостей (замер v2: 320 номеров, вид «Месяц», 60 fps без пропусков), `TransferAdminOrdersContent`, недостижимые `RepresentativeAdminContent` / `TransferAdminContent`; живые `EditReserveDate` и `TransferAdminDriversContent` переехали в `Blocks/`. Lint-бэйзлайн 7876.
- Доступы: `canManageAirlineAccess(accessMenu, user)` в `src/utils/access.js` — `DISPATCHERADMIN` правит доступы авиакомпаний без ключа `accessManage` (4 точки АК-контекста, рендерер правил `DispatcherAdminContent` получил `guard`-функцию) (`f0c5442`); `resolveEffectiveAccessMenu` вынесен в utils с тестами, эффективные права считаются для всех ролей, а не только диспетчера и АК (`577c65e`).
- TravelLine: `SyncIndicator` в шапке раздела, авто-синхронизация без модалки прогресса (`bab16ba`); на бэке синхронизация каталога — крон (`fe04df4`). Цены трансфера: поиск по названию договора и всем вместимостям через `matchesTransferPriceSearch` в `src/utils/transferPrices.js` (+7 тестов), мёртвый `TransferPricesForm` удалён, липкий поиск реестра ФАП (`ceaf65d`).
- Релиз: патч-ноут **4.4.0** в `scripts/patchNotes.data.mjs`, шаблон «Что нового» 4.4.0 в `scripts/systemUpdate.data.mjs`, `ver 4.4.0` в четырёх ролевых меню.